# T8.6a 验收记录（Qwen Code 适配器）

**执行时间：** 2026-09-05
**执行人：** 验收子 Agent（独立验收，边验边写）
**被验提交：** `827c58b`（`feat: T8.6a Qwen Code 适配器 —— 调研 + fixture 回放 + 实现装配 + 真机冒烟`，HEAD）
**依据：** `docs/开发进度.md` §0「T8.6a ✅ 完成（待验收）」（交付报告）与 §4.5 更新行；`docs/adapters/qwen-code.md`（调研文档）；`docs/开发计划.md` §16.4 T8.6a 行（合同）与 §1.4；`docs/验收记录/T7.3a-验收.md`（同类适配器验收款式）与 `T8.5c-验收.md`（最新款式）
**主管理员对交付方三条裁定请求的结论（验收如实登记，落档动作归下一单）：** ① opencode env 指纹 flake 触发率上升（7 跑 3 触发）——**下一单顺手加固**（时序钉子或该用例局部串行化，方案由实现方选并落档理由）；② vitest worker fork flake——暂不动 pool 配置，维持观察（形态已抓到，复发频繁再立单）；③ qwen 权限转发翻案路径——跟踪上游，无动作
**版本关口：** Phase 8（M3）内工单，不构成版本关口

---

## 0. 执行环境

| 项 | 值 |
|---|---|
| 平台 | Windows 10（10.0.22631），PowerShell，全部命令在仓库根执行 |
| 包管理器 / Node | pnpm 11 · Node 24 · Electron monorepo（vitest，biome） |
| 被验 HEAD | `827c58b`。开工时 `git status --short` 仅两条未跟踪 iflow 产物（`docs/adapters/iflow.md`、`packages/adapters/fixtures/iflow/`，T8.6b 并行调研单产出，本验收全程不动） |
| 真机 | qwen-code 0.23.0 本机全局安装（npmmirror 镜像）——live 脚本可实跑（内嵌假 OpenAI 兼容服务端 + CLI 全真） |
| 跨包重建 | live 脚本消费 `packages/adapters/dist`，实跑前 `pnpm --filter @ff-pane/adapters build`；反向探针经 vitest 直测 TS 源（adapters 测试引 `../src/index.js` 源路径、desktop 测试引 desktop 源），跨包探针（若涉及 desktop 消费 adapters dist）先重建、结束还原重建回绿 |
| 已知 flake（§4.5 名单四条） | adapters generic-exec cancel · E2E task-parallel 第 3 条 · vitest worker fork 意外退出（第三条）· opencode env 指纹用例（第四条，**近期触发率上升，交付方 7 跑 3 触发**）——触发则复跑绿即登记 |
| 并行情况 | T8.6b（iFlow）调研由另一子 Agent 进行，其两项未跟踪产物在工作区，验收结束时应原样仍在 |

---

## 1. 逐条核验（读码为证，不采信交付报告自证）

### A. 调研质量（`docs/adapters/qwen-code.md`，336 行通读）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 关键结论有实测依据标注 | ① **协议 Claude 信封重写**：§0 对照表（`qwen-code.md:20-28`）三层分叉（信封形态/参数面/退出码语义），信息来源三源标注（本机真机 + 安装包同版官方文档 + 安装包 JS 源码，:6-8）；② **OAuth 免费层废止**：:9-11 头部认证限制声明明示出处（0.23.0 官方文档 auth.md + `/auth` 菜单已移除），§6 表格 qwen-oauth 行再证；③ **三个伪装成功坑**：§8 坑 1/2/3（:223-235）逐条标注真机实测（坑 1「真机实测 401」、坑 2 引 §3.4 真机行为、坑 3「本机 6 跑 3 现」非确定性频率如实登记）；④ **能力六项**：§9 核对表（:255-262）每项「依据」列注明真机实测或源码出处 | ✅ |
| 「先出调研再实现」纪律痕迹 | 调研文档自成一体（§0~§10），实现决策（§9.1 两案对比、§9.2 命令模板、§9.3 映射表 + end 判定五规则）全部在调研内先落档；实现文件头注（`mapper.ts:9-21`、`command.ts:6-20`、`adapter.ts:9-21`）逐条回指调研章节——实现是调研的投影而非反向补写。§16.4 合同「先出调研再实现」的落点文件清单（`qwen-code.md` 列首位）与交付一致 | ✅ |
| 两案对比论证质量 | §9.1（:264-281）：案 A（独立目录 + 复用共享模块）选定；案 B（派生自 gemini-cli）弃因引 §0 对照表「协议不同款、无共享面」；案 B'（派生自 claude-code）弃因具体到语义层五轴差异（工具名 / diff 来源 / 成败判据 / 控制协议可用性 / args）+「共享映射器需三轴参数化、抽象面大于手写面」的量化判断 +「把已验收的 claude 双向协议实现改双形态引入回归风险」——论证独立自足，非口号式取舍 | ✅ |
| 细节诚实度抽查 | 官方文档退出码 41（FatalAuthenticationError）与 0.23.0 源码不符（grep 零命中）被如实登记（:90-92）「文档滞后于实现，按实测语义处理」；§10 待真机补验四项（真实后端字段 / --mcp-config / 双向模式 / --acp）逐项写明前置条件——不掩盖未验面 | ✅ |

### B. 三坑防线（本单最要紧，读实现核实 + 单测对应）

| 防线 | 实现证据 | 单测证据 | 结论 |
|---|---|---|---|
| ① `[API Error:` 文本 + result success + 退出码 0 → failed | 标记常量 `QWEN_API_ERROR_MARKER = "[API Error:"`（`native.ts:69`，注明 0.23.0 源码字面量出处与「漂移症状是漏判 completed、fixture 钉住字面量」）；三处命中检测：stream_event 增量（`mapper.ts:185-187`）、assistant 行 text/thinking 块（:351-353，该块虽不产出事件仍用于标记检测）、result.resultText（:374-376）；finish() 规则 3（:405-411）：`sawApiErrorMarker` → `reason="failed"` 且 message 注明「result 仍报 success、退出码仍 0」——判定优先级在 is_error/subtype 检查**之前**、在 permission_denials 之后 | `qwen-code.test.ts:248-255`：回放 `real-stream-json-api-error.jsonl`（fixture 末行实为 `subtype:"success"`、`is_error:false`，经 Node 直读独立核实）+ 退出码 0 → 断言 end.reason=failed 且 message 含 "API Error" | ✅ |
| ② `result.permission_denials[]` 结构化消费 | native 收窄（`native.ts:313-326`：`permission_denials` 数组逐条收 `tool_name`/`tool_use_id`，非对象条目丢弃）；finish() 规则 2（`mapper.ts:397-404`）：`result.permissionDenials.length > 0` → failed，message 列出去重后的被拒工具名——**在 subtype/is_error 检查之前**，故 fixture 里 `subtype:"success"` 完全不影响改判；动作级 denied 改判另一路：`mapper.ts:264-265` `isError && isQwenDenialText(content)` → status="denied"（文本判据 `native.ts:58-61` 两条正则，注释明示「整轮成败不靠这段文本，denials 是结构化判据」） | `qwen-code.test.ts:231-246`：回放 `real-stream-json-headless-deny.jsonl`（fixture 末行经 Node 直读独立核实：`subtype:"success"`、`is_error:false`、`permission_denials` 含 write_file + run_shell_command 两条）→ 断言 file_change 与 command 各有 denied、end.reason=failed、message 同含两个工具名 | ✅ |
| ③ result 已到达时退出码不参与判定 | finish() 判定链（`mapper.ts:386-424`）：cancelled → spawn-failed/killed → result 缺席（crashed，「退出码此时参与兜底」）→ 规则 2 → 规则 3 → is_error/subtype → **规则 4（:420-423）全过且 result 到达 → completed，`outcome.exitCode` 全程不进任何判定分支**，仅在 :436 原样留档进 end.exitCode。result 缺席时（:393-396）exitCode 同样只留档、reason 由 endKind 决定——「参与兜底」的实际形态是流截断即 crashed（无论退出码），比 gemini 款式更保守且不会把崩溃退出码误读 | `qwen-code.test.ts:304-313`：回放 success fixture + `exitCode: -1073740791`（0xC0000409 真实值）→ 断言 reason=completed 且 exitCode 原样留档；:315-325 反面：killed fixture（无 result）+ exitCode 1 → crashed 且 message 含「result」 | ✅ |

三道防线各自的反向验证见 §3 探针 ①②③（各恰红于对应单测）。

### C. 能力六项如实性（声明 `adapter.ts:53-60` vs 调研 §9 核对表逐项）

| # | 能力 | 声明 | 调研依据核实 | 结论 |
|---|---|---|---|---|
| 1 | nativeResume | yes | `--resume` 真机实测 session_id 复用（fixture resume/success 两份 init 行 session_id 经 Node 直读核实一致 `2d2a0f7e-…`）；强杀后凭据可用（killed fixture 首行即 init 带 session_id）；live 判据 10 实跑再证 | ✅ |
| 2 | streaming | yes | success fixture 含 `content_block_delta` 两片真增量（单测 :194-204 断言拼接完整且恰 2 片）；命令行恒带 `--include-partial-messages`（`command.ts:112-115` 固定段） | ✅ |
| 3 | fileChangeEvents | partial | tool_result 无 diff 正文（fixture user 行 content 为纯文本「Successfully created…」）；仅 edit 可从 old/new 渲染（`mapper.ts:221-224` 条件 `block.name === "edit"`），write_file 不自造（单测 :172-182 断言 write_file 的 diff 为 undefined） | ✅ |
| 4 | commandEvents | partial | `mapper.ts:290-302` command 事件**无 exitCode 字段可赋**（注释:297 明示无结构化退出码，gemini 同款评级）；单测 :184-192 断言 exitCode 恒 undefined | ✅ |
| 5 | permissionForwarding | **no** | 调研 §3.4（:149-154）：`control_request(can_use_tool)` 通道源码存在但明确分流——`inputFormat !== "stream-json"` 时按本地审批模式立即裁决不发请求；官方标注双向 SDK 模式 under construction；本适配器是单发 headless（`--input-format stream-json` 不下发，`command.ts` 固定参数段核实无该参数）。实现侧 `startTurn` 返回值无 `respondPermission`（`adapter.ts:233-239`，单测 :357-366 断言 toBeUndefined）。翻案路径已入 §4.5（进度文档 :1344 行核实），主管理员裁定③「跟踪上游，无动作」 | ✅ |
| 6 | gracefulCancel | partial | `cancel()` 实现只 `handle.kill()` 树杀（`adapter.ts:235-238`）；调研 §5：headless 下 CLI 不装取消监听，interrupt 归双向模式；强杀后会话可续（killed fixture + 调研 §4 实测） | ✅ |

**无 CLI_LOGIN_RUNTIMES 加入的核实**：`auth-probe/types.ts:9-15` 清单仍为五家（codex/claude-code/gemini-cli/opencode/grok-build），零 qwen-code；`git diff 14a697a 827c58b --stat -- packages/adapters/src/auth-probe` 零输出。依据成立：调研 §6（:195-203）——`qwen auth` 子命令 0.23.0 已整体移除（帮助标注 removed）、qwen-oauth 已废止、settings.json 里的 API key 属用户自管配置非 CLI 登录态——cli_login 语义无对应物，不硬造探测规则。✅

### D. 实现与装配

| 核查点 | 证据 | 结论 |
|---|---|---|
| `src/qwen-code/` 结构照仓内款式 | 5 文件 `{native,command,mapper,adapter,index}.ts`（native 收窄 / command 纯函数 / mapper 有状态映射 / adapter 门面 / barrel）——与 gemini-cli、grok-build 目录款式同构；全部经共享模块（`readJsonlStream`/`decodeLines`/`spawnAgentProcess`/`toRawEvent`/`createLiteralGuard`）复用，无自造平行件 | ✅ |
| 提示词恒走 stdin | `adapter.ts:168-183`：spawn spec `stdin: "pipe"` + `handle.stdin.end(ctx.prompt, "utf8")`；args 构建（`command.ts:99-145`）**无任何 positional 提示词与 `-p`**；理由注释（`command.ts:18-20`）指向 .cmd 垫片截断坑（调研 §8 坑 5，codex 同款） | ✅ |
| spawn 经 spawnAgentProcess | `adapter.ts:34` 导入 + :139 `options.spawn ?? spawnAgentProcess`（测试接缝款式），qwen-code 目录内无第二个 spawn/child_process 入口（grep 核实）；spawnAgentProcess 内 spawn 后立即入 Job（既有机制，T8.5c 验收 §1-C 已核） | ✅ |
| 装配四处 | ① `KNOWN_RUNTIMES` +"qwen-code"（`src/adapter.ts` diff +1 行）；② registry 裸键单例：`registry.ts:180-181` `registry.register(createQwenCodeAdapter())` 零构造期配置 + 头注零配置清单补 qwen-code（diff 核实），注释明示「一轮一 spawn 无常驻资源不参与 hasRuntimeResources/closeRuntimes」——与 codex 款式一致；③ `env.ts`：`runtimeApiKeyEnvVar` +case "qwen-code" → OPENAI_API_KEY（注释引调研 §6 OAuth 废止依据）、`runtimeBaseUrlEnvVar` 三元并入 qwen-code → OPENAI_BASE_URL；④ Profile 下拉：`ProfileEditorDialog.tsx` RUNTIME_OPTIONS +"qwen-code" 恰 1 行 | ✅ |
| process/env.ts 清洗 +QWEN_MODEL | `/^QWEN_MODEL$/i` 一条（diff 恰 +3 行含注释），注释复述「OPENAI_MODEL 已被 ^OPENAI_ 剥掉，别名不剥等于留后门」；QWEN_CODE_* 行为开关不入清单（调研 §7 依据：不携带凭证，GOOGLE_GENAI_USE_GCA 先例） | ✅ |
| 零 i18n 新键口径 | 下拉渲染 `{rt}` 技术标识符原文（`ProfileEditorDialog.tsx:147-151` 读码核实，非 i18n 键）——grok-build（T7.3a 验收 §1-9）与 opencode 同款先例；本单 diff 无 locales 文件 | ✅ |
| strict TS | 可选字段全程条件展开（`mapper.ts` toUsage/end 组装、`adapter.ts:155-179` spawn spec、`native.ts` optional() 辅助）——exactOptionalPropertyTypes 款式与仓内一致 | ✅ |

### E. fixtures（6 份新录制 + README）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 6 份新录制的性质与脱敏 | 字节级核验（Node 直读）：6 jsonl + README 全部 **LF、无 BOM、无 UTF-16、jsonl 内零 `admin`**（README 出现 2 处 `admin` 是脱敏说明本身「admin → USER」）；关键结构独立核实：deny 末行 `subtype:"success"`/`is_error:false`/denials=[write_file,run_shell_command]、api-error 末行 `subtype:"success"`/`is_error:false`/result 文本 `[API Error: 401 …]`、killed 恰 2 行（system+stream_event 无 result）、success 24 行以 result 收尾、resume 与 success 的 init session_id 一致——与 README 文件清单逐项吻合 | ✅ |
| README 注记（UTF-16 坑） | README「复现录制」节明示「cmd 重定向保证 stdout 原始字节，PowerShell 的 > 会写成 UTF-16，录 fixture 不可用」；「性质说明」节把真（CLI 行为/信封/退出码/落地）与假（模型回话内容）与未覆盖（真实后端字段）三档划清，auth-missing 标注全真 | ✅ |
| 既有 fixtures 逐字节不动 | `git diff 14a697a 827c58b --stat -- packages/adapters/fixtures/` 输出**仅 7 个 qwen-code/ 新增文件（143 insertions, 0 deletions）**，无任何既有文件出现 | ✅ |
| `tools/fake-openai-server.mjs` 零改动 | 同一 stat 过滤 `fixtures/tools/` **零输出**；live 脚本 `live-qwen-code.mjs:32` 直接引用该既有脚本（复用而非改动） | ✅ |

### F. 单测（新 28 条断言强度抽查）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 28 条计数 | `tests/qwen-code.test.ts` 逐 describe 清点：命令行组装 6 + edit diff 3 + 成功流回放 6 + resume 回放 1 + 权限拒绝防线 1 + API 错误防线 1 + 错误与截断 6 + 适配器本体 4 = **28**，与交付报告一致 | ✅ |
| fixture 回放是真回放 | `replay()`（:50-62）：`readFile` 真读 fixture 文件 → `readJsonlStream`（W2.1b 真管道，非手拆行）→ 真 mapper → finish——**不是**内联 JSON 假数据；断言直接消费统一事件（如增量拼接 `"Created hello.txt and checked the Node version."` 恰 2 片、usage.totalTokens=2571 取自 fixture 原值） | ✅ |
| 三坑各有钉子 | 坑① :248-255（api-error fixture + message 含 "API Error"）；坑② :231-246（denied 改判 + 整轮 failed + message 双工具名）；坑③ :304-313（**真实崩溃码 -1073740791** + completed + exitCode 原样留档）——三条钉子的反向可验性见 §3 探针 ①②③ | ✅ |
| 断言强度抽查 | 命令行组装首条（:72-86）是 `toEqual` 全序列断言（非 arrayContaining 弱断言）；「assistant 行 text 块不重复产出」用增量拼接 + 恰 2 片双重断言（:198-203，若双路产出则全文两遍立刻红）；「不自造 diff」断言 `diff` 为 undefined 而非仅存在性；cwd 快速失败用例带 `spawned === 0` 计数（:368-389，证明失败先于 spawn） | ✅ |
| session-registry 参数化第六家 | diff 核实：`it.each([...,"opencode","qwen-code"])` 零配置裸键单例第六家；未注册样例由 qwen-code 换 iflow（T8.6b 未交付，恰仍是真实未注册项）——参数化 +1、样例改写，与交付报告「1 增 1 改」一致 | ✅ |
| session-env +2 | diff 核实：`runtimeApiKeyEnvVar("qwen-code") === "OPENAI_API_KEY"` 断言并入既有用例 + 新用例「qwen-code 同走 OPENAI_API_KEY + OPENAI_BASE_URL」（dashscope 样例 baseUrl 全等断言） | ✅ |

### H. 红线（`git diff 14a697a 827c58b --stat`，24 文件 +2373/−10）

| 红线 | 证据 | 结论 |
|---|---|---|
| 依赖 / lock 零改动 | 过滤 stat（全部 package.json + pnpm-lock.yaml + pnpm-workspace.yaml）**零输出**（qwen CLI 是全局 npm 安装，不进仓库） | ✅ |
| 既有适配器 src 改动面 | 过滤 stat（codex/claude-code/gemini-cli/grok-build/opencode/aider/generic-exec/acp/auth-probe/events/guard 全部目录）**零输出**；共享面仅 `src/adapter.ts` +1 行（KNOWN_RUNTIMES）、`src/process/env.ts` +3 行（QWEN_MODEL 清洗一条含两行注释）、`src/index.ts` +2 行（barrel 导出）——逐行读 diff 核实无逻辑改动 | ✅ |
| 24 文件与交付报告落点一致 | stat 逐一对照交付报告落点段：调研文档（新）· qwen-code 5 源文件（新）· adapters 3 处装配 · fixtures 7 文件（新）· live 脚本（新）· qwen-code.test.ts（新）· desktop 4 文件（registry/env/ProfileEditorDialog/两测试）· 进度文档——**完全吻合，无未声明落点** | ✅ |
| §1.4 非 MVP 红线 | 新增文件 grep `TODO|FIXME|XXX` 零命中；新逻辑全带单测（+30）；本单零 i18n 新键；未 push（`main [ahead 22]`） | ✅ |
| 并行红线 | iflow 两项未跟踪产物全程未动（验收结束时 status 复核见 §6） | ✅ |

### G. live 冒烟（`live-qwen-code.mjs`，188 行通读 + 本验收实跑）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 10 判据设计合理性（逐条评估「是否只测假服务端自己」） | 判据 1/2（end 恰一条 completed）——适配器 + CLI 终局语义；3（**原生会话 ID = 预生成 --session-id**）——纯 CLI 侧（init 行 session_id 由真 qwen 回显，验证预生成登记通道）；4（session_start 先于一切动作）——事件次序；5（文件变更完成带路径）——CLI 工具层；6（**hello.txt 真落地 + 内容含 hello**）——文件系统级证据，最硬一条（yolo 真放行）；7（命令带输出 v* 且 **exitCode 如实缺席**）——真执行 node -v + 能力声明 partial 的正面自证；8（token 级增量 >1 片 + final 收尾）——增量切分是 CLI 的 stream_event 投影行为；9（usage 汇总 >0）——result 行真实字段；10（**resume 第二轮实跑会话 ID 复用**）——纯 CLI 侧（--resume 真机语义，第二轮独立 spawn）。结论：**没有一条只测假服务端自己**；与单测的分工在头注（:6-9）明确（单测管映射、live 管「命令行真能启动 / stdin 真被读 / yolo 真落地 / resume 真复用」） | ✅ |
| 密钥经 ctx.env 注入与 desktop 同通道 | 脚本 :73-74 注释声称「desktop 装配的同一条通道：resolveRuntimeEnv → ctx.env → buildAgentEnv 注入优先于清洗」。独立核实：脚本把 `OPENAI_API_KEY`/`OPENAI_BASE_URL` 经 `startTurn({ env: injectedEnv })` 传入（:90-97）→ 适配器 `adapter.ts:174-176` `ctx.env` 进 spawn spec 的 `env` → 与 desktop 侧 `orchestrator` 装配 `resolveRuntimeEnv("qwen-code")`（`env.ts` 返回同两个变量名）→ `turnCtx.env` 的路径**变量名与传递口完全一致**——声称属实 | ✅ |
| 本验收实跑 | `pnpm --filter @ff-pane/adapters build`（exit 0）后实跑：**10 判据 ALL PASS，exit 0**（约 9 s）。真机 qwen 0.23.0 + 内嵌假 OpenAI 兼容服务端（`fixtures/tools/fake-openai-server.mjs` 直接复用）；预生成会话 ID `c867495e-…` 与 init 回显一致；hello.txt 真落地；node -v 输出 v24.15.0 且 exitCode 缺席；文本恰 2 片增量 + final；usage.totalTokens 2571；resume 第二轮 completed 且会话 ID 复用 | ✅ |

---

## 2. 抽查发现

**未发现影响结论的缺陷，红线全部未破。** 以下如实登记：

1. **（覆盖口径观察，不影响结论）`/^QWEN_MODEL$/` 清洗条目无专属单测钉子。** 全仓 grep：`QWEN_MODEL` 仅出现在 `process/env.ts:40`（实现）与两份文档，测试零命中。§3 探针④证实：删除该条目后 `process.test.ts`（46 条含清洗模式清单用例）与 `session-env.test.ts` **全绿不红**——既有清单用例（`process.test.ts:396-406`）只抽查了前缀型/兜底型样例与 `length > 10` 的粗粒度下界，未钉住该单点。交付报告未声称为它写了测试，不算声明不符；作为覆盖缺口登记（同类先例：T7.3a 验收 §2-3 grokBuildRule 无专属分支单测）。机制正确性经探针的反向核实为真：条目在（`isApiKeyEnvName("QWEN_MODEL")` 命中 `/^QWEN_MODEL$/i`）、删掉后无人拦——建议下一单顺手补一行断言（一行 `expect(isApiKeyEnvName("QWEN_MODEL")).toBe(true)` 即可）。
2. **（正面观察）API Error 标记检测覆盖三条到达路径。** 增量（delta 文本）、assistant 行 text/thinking 块（该块不产出事件但仍参与标记检测，`mapper.ts:350-353`——防「关 partial 时标记只在 assistant 行」的形态缺口）、result.resultText 三处都设了检测（:185-187/:351-353/:374-376），fixture 的 api-error 流恰好三路都含标记，任一路漏检都还有两路兜住。防线不是单点。
3. **（正面观察）「result 缺席时退出码参与兜底」的实际形态比声明更保守。** finish() 中 result 缺席一律 crashed（`mapper.ts:393-396`），退出码只留档不改变 reason——不存在「退出码 0 且流截断 → completed」的误判窗口。声明「照常参与兜底判定」的实义是「留档 + endKind 判定」，无美化。
4. **（设计观察，不影响结论）tool_result.content 仅收窄字符串形态。** `native.ts:223-231` 注明「content 观测为字符串（真机 fixture）；对象/数组形态按 unknown 处理不硬解」——真实后端（thinking 块、多模态）可能出现数组形态 content，届时 tool_result 的 content 缺席、动作事件无 output 但状态仍正确（isError 独立字段）。已被调研 §10 待验项覆盖（用户提供 key 后补录核对），仅登记备查。

---

## 3. 反向验证（5 条探针，全部编辑器工具改 → 跑对应测试文件 → 观察 → `git checkout --` 还原 → 回绿）

基线：四目标文件（`qwen-code.test.ts` 28 + `process.test.ts` 34 + `session-registry.test.ts` 24 + `session-env.test.ts` 12）共 **98 条全绿**。adapters/desktop 测试均直测 TS 源，探针无需跨包重建；live 实跑前统一重建 dist。

| # | 探针 | 结果 |
|---|---|---|
| ① | API Error 升格防线破坏：`mapper.ts` finish() 删除 `sawApiErrorMarker` 分支（照 result 报 success） | **1 failed / 27 passed**——恰为「[API Error: 文本标记 → 整轮 failed」（expected 'completed' to be 'failed'）。防线被钉死 |
| ② | permission_denials 消费删除：finish() 删除 `permissionDenials.length > 0` 分支 | **1 failed / 27 passed**——恰为「default 模式：被拒动作记 denied，整轮 failed 而非 completed」。结构化判据被钉死 |
| ③ | 退出码防线破坏：finish() 规则 4 前插入 gemini 款式「非零退出码 → failed」分支 | **1 failed / 27 passed**——恰为「result 已到达时退出码不参与成败判定（0xC0000409 不误判）」（received 'failed'）。libuv 崩溃防线被钉死 |
| ④ | env 清洗删 `/^QWEN_MODEL$/`（`process/env.ts` 三行删除） | `process.test.ts` 34 条 + `session-env.test.ts` 12 条**全绿不红**——**该条目无单测钉子**（工单预期「session-env 单测红」不成立：session-env 测的是 desktop 注入面（OPENAI_API_KEY/BASE_URL），清洗面归 adapters process.test.ts，而后者的清单用例未含 QWEN_MODEL 样例）。**即为覆盖缺口发现**，登记 §2-1；条目本身在（实现读码 + 正则命中人工核实） |
| ⑤ | registry 装配删 qwen-code：`registry.ts` 删 `registry.register(createQwenCodeAdapter())` | **1 failed / 23 passed**——恰为参数化第六家「qwen-code：resolveForProfile 返回 registry.get 的同一实例」。装配被钉死 |

5 条探针，4 条恰红于目标用例 + 1 条未红即为覆盖缺口发现（满足「至少 4 条」；④ 的未红本身是有效验收产出）。每条还原后复跑回绿；全部结束后四文件基线 **98/98 回绿**，`git status --short` 仅 iflow 两项 + 本验收记录。

---

## 4. 自动化回归结果

全部在仓库根执行，HEAD = `827c58b`（探针全部还原后；live 实跑前 dist 已重建）。

| 项 | 结果 |
|---|---|
| 单测独立连跑 | **2057 / 2057（100 文件）× 3**，耗时 19.14 / 19.27 / 20.40 s——**三跑全绿，§4.5 四条已知 flake 均未触发**（含近期触发率上升的 opencode env 指纹用例与 vitest worker fork 形态） |
| `pnpm lint` | exit 0 —— biome `Checked 599 files. No fixes applied.` + `[check-i18n] PASS` |
| `pnpm build` | exit 0 —— 6 工程三套 typecheck，renderer 产物 1,552.15 kB（与交付报告一致） |
| `pnpm smoke` | exit 0 —— 七项全 PASS（main-sqlite · ipc-ping-pong · app-info · sqlite-via-ipc · event-subscription · csp-blocks-eval · secrets-roundtrip），**`No handler registered` 0 行**（Select-String 计数独立核实 = 0） |
| `pnpm test:e2e` | **29 passed（22.4 s）**，exit 0，一次通过 |
| live-qwen-code.mjs | **10 判据 ALL PASS**，exit 0（真机 qwen 0.23.0 + 内嵌假模型服务端 + resume 第二轮实跑；详见 §1-G） |

---

## 5. 遗留 / 建议登记的技术债

| 项 | 说明 | 建议归属 |
|---|---|---|
| `/^QWEN_MODEL$/` 清洗条目无单测钉子 | §2-1 / §3 探针④：删除后 process.test.ts 与 session-env.test.ts 全绿不红——机制人工核实为真，缺一行断言 | 下一单顺手补（一行 `expect(isApiKeyEnvName("QWEN_MODEL")).toBe(true)` 即可，与主管理员裁定①的 opencode flake 加固同车） |
| tool_result.content 仅收窄字符串形态 | §2-4：真实后端数组形态 content 届时按 unknown 处理（状态判定不受影响），调研 §10 待验项已覆盖 | 备查——用户提供 DASHSCOPE_API_KEY 补录时一并核对 |
| 真实 Qwen/ModelStudio 后端字段未录 | 调研 §10 / fixtures README 已逐项登记（thinking 块形状、stop_reason 取值域、result.stats）；live 脚本 `LIVE_REAL_MODEL=1` 通道已备好 | 待用户提供 API key，既定用户介入项，不阻塞本单 |
| qwen 权限转发 / 优雅取消翻案路径 | `--input-format stream-json` 双向模式（官方 under construction）与 `--acp`——§4.5 已登记（进度文档 :1344） | **主管理员已裁定：跟踪上游，无动作** |
| opencode env 指纹 flake 触发率上升（7 跑 3） | 本验收 3 连跑未触发，但交付方数据如实 | **主管理员已裁定：下一单顺手加固**（时序钉子或该用例局部串行化，方案由实现方选并落档理由） |
| vitest worker fork flake | 形态已锁定（§4.5 第三条），本验收未触发 | **主管理员已裁定：暂不动 pool 配置，维持观察，复发频繁再立单** |

---

## 6. 验收结论

### **通过（带遗留）。**

- **A 项成立**：调研文档 336 行三源标注（真机 + 同版官方文档 + 安装包源码），四组关键结论（协议信封重写 / OAuth 废止 / 三坑 / 能力六项）逐条有实测出处；「先出调研再实现」纪律痕迹清晰（实现文件头注回指调研章节，决策全部先落调研 §9.x）；两案对比（§9.1）弃因具体到语义层五轴差异与回归风险，论证独立自足；官方文档退出码 41 与源码不符被如实登记。
- **B 项成立（本单最要紧）**：三坑防线全部读码闭合且各有单测钉子——① `[API Error:` 标记三路检测（增量/assistant 块/resultText）+ finish() 规则 3 改判 failed（探针①恰红）；② `permission_denials[]` 结构化收窄 + 规则 2 优先于 subtype/is_error 判定（fixture 末行 `subtype:"success"` 经 Node 直读独立核实，探针②恰红）；③ result 已到达时 exitCode 全程不进判定分支、仅留档（真实崩溃码 -1073740791 用例，探针③恰红）。
- **C 项成立**：六项声明与调研 §9 核对表逐项一致且不美化（fileChange/command 双 partial 的「不自造 diff / exitCode 恒缺席」有实现与单测双证；permissionForwarding no 的源码分流依据 + 实现无 respondPermission + §4.5 翻案路径登记三层闭合）；CLI_LOGIN_RUNTIMES 未加入且依据成立（auth 子命令已移除，不硬造探测规则）。
- **D 项成立**：5 文件结构照仓内款式、共享模块复用无平行件；提示词恒走 stdin（args 无 positional 无 `-p`）；spawn 唯一入口经 spawnAgentProcess；装配四处逐 diff 核实（KNOWN_RUNTIMES / registry 裸键单例 / env.ts 双变量 / Profile 下拉）；零 i18n 新键口径与 grok-build 先例一致。
- **E 项成立**：6 份 fixture 字节级核验（LF / 无 BOM / 无 UTF-16 / jsonl 零 admin），关键结构（deny 的 denials 双条目、api-error 的 401 文本、killed 的 2 行截断、resume 与 success 的 session_id 一致）与 README 清单逐项吻合；README 含 UTF-16 录制坑注记与真假三档性质说明；既有 fixtures 与 `tools/fake-openai-server.mjs` 过滤 stat 零输出。
- **F 项成立**：28 条计数吻合；回放是真管道（readFile + readJsonlStream）非内联假数据；三坑各有钉子且反向可验；session-registry 参数化第六家 + 未注册样例换 iflow、session-env +2 均 diff 核实。
- **G 项成立**：live 10 判据无一「只测假服务端自己」（判据 3/6/10 是纯 CLI 侧 / 文件系统级证据）；密钥 ctx.env 注入通道与 desktop 装配同一条的声称读码核实属实；本验收实跑 **ALL PASS**（含 resume 第二轮会话 ID 复用）。
- **H 红线未破**：依赖/lock 零改动；既有适配器 src 目录全部零输出，共享面仅 KNOWN_RUNTIMES 一行 + env 清洗一条 + barrel 两行；24 文件与交付落点完全吻合；无 TODO；未 push；iflow 两项并行产物全程未动。
- **反向验证**：5 条探针——①②③⑤ 恰红于目标用例（三坑防线 + registry 装配全部被钉死），④ 未红即为覆盖缺口发现（QWEN_MODEL 清洗无钉子，§2-1 登记）。还原后基线 98/98 回绿。
- **回归数字**：2057/2057 × 3（100 文件，四条已知 flake 均未触发）· lint exit 0（599 文件 + check-i18n PASS）· build exit 0 · smoke 七项 PASS 且 `No handler registered` 零行 · E2E 29/29 一次通过 · live 10 判据 ALL PASS。

**主管理员已裁定事项（如实登记，落档动作归下一单）**：① opencode env 指纹 flake——下一单顺手加固（时序钉子或局部串行化，实现方选并落档理由）；② vitest worker fork flake——暂不动 pool 配置维持观察；③ qwen 权限转发翻案——跟踪上游无动作。

**需主管理员裁定事项**：无新增。建议（非裁定）：QWEN_MODEL 清洗钉子一行断言随下一单顺手补（§5 首行）。

验收结束时 `git status --short`：

```
?? docs/adapters/iflow.md
?? docs/验收记录/T8.6a-验收.md
?? packages/adapters/fixtures/iflow/
```

