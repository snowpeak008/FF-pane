# T8.4 验收记录（自定义角色）

**执行时间：** 2026-09-03
**执行人：** 验收子 Agent（独立验收）
**被验提交：** `42f1da5`（`feat: T8.4 自定义角色 —— 角色定义存储 + 权限预设校验（§7 清单不可绕过）+ Prompt 第 1 层接入 + 设置页管理`，HEAD）；其前一提交 `ac3cd2d` 为 T8.3b 验收落档（文档，不在本次范围）
**依据：** `docs/开发计划.md` §11 T8.4 合同（名称 + 角色提示词 + 权限预设 + Prompt 第 1 层接入 + 存储与 Profile 绑定 + 界面管理入口；验收判据：自定义角色建 Profile 跑通一轮真实会话，权限预设生效）、§16.4 表 T8.4 行（红线沿用 T7.2：信封交集，**不得绕过 §7 危险操作固定清单**）、§1.4 非 MVP 红线、§1.5 界面交互质量要求；`docs/开发进度.md` §0「T8.4 ✅ 完成（待验收）」（交付报告，含半成品续用说明）；`docs/验收记录/T8.3b-验收.md`（款式沿用）
**特殊背景：** 交付系两任接力（前任中断留半成品约 20 文件、现任评估后续用并补完）。验收不区分作者、对最终提交整体负责；「续用评估是否靠谱」经死代码抽查核实（§2-H）。
**版本关口：** Phase 8（M3）单一工单，不构成版本关口

---

## 0. 执行环境

| 项 | 值 |
|---|---|
| 平台 | Windows 10（10.0.22631），PowerShell，全部命令在仓库根执行 |
| 包管理器 / Node | pnpm 11 · Node 24 · Electron 44（vitest 4.1.11，biome 578 文件） |
| 被验 HEAD | `42f1da5`，开工时 `git status --short` 为空。验收全程**未改一行产品代码留存**：§4 五条反向探针均以编辑器工具修改、`git checkout -- <file>` 逐字还原，每次还原后 `git status --short` 输出为空（无 CRLF 残留，未触发「删除 + checkout」流程）；A/B 项两个临时单测探针与真机演示脚本用完即删 |
| core/shared 重建纪律 | 探针②（core 源码）与⑤（shared 源码）涉及被 dist 消费的包：改后按需 `pnpm --filter <pkg> build` 再跑 desktop 侧，还原后重建回绿（§4） |
| 自动化 | vitest 1911 单测 / 95 文件 ×3 + 探针 8 次单文件运行 + 基线复跑 267/267 + 临时探针测试 2 个（8 用例，全绿后删除）· lint · build · smoke 七项 · E2E 27 条全量 ×2 + task-parallel 单 spec ×1 |
| 真机 | **本次执行了真机演示**（工单裁定 1 的顺带项）：claude-code（登录态可用）跑通一轮自定义角色讨论，详见 §5。codex 有登录态但本机 headless `codex exec` 两次挂起（3.5 分钟以上零输出，环境问题），未采用。演示走临时 dataRoot / userData / 项目根，用后即弃，脚本已删 |
| 已知 flake | adapters `generic-exec` cancel 用例——本次未触发。E2E `task-parallel` 第 3 条——第 1 次全量跑触发一次（负载性超时，全程 2.7 分钟），按既有口径复跑绿即登记（§6.1） |
| 并行情况 | 无其他 Agent 并行 |

---

## 1. 逐条核验（读码为证，不采信交付报告自证）

### A. 领域与向后兼容

| 核查点 | 证据 | 结论 |
|---|---|---|
| `RoleRef = Role \| CustomRoleId` 联合 + `role-` 前缀判别 | `shared/domain/profile.ts:55`（联合类型）、`:34` `CUSTOM_ROLE_ID_PREFIX = "role-"`、`:41-47` `isCustomRoleId`（前缀 + 非空随机段，`"role-"` 裸前缀拒绝）、`:58-60` `isRoleRef = isRole \|\| isCustomRoleId`。`CustomRoleId` 品牌类型在 `common.ts:39` | ✅ |
| `Role` 闭合三字面量未被打开 | `profile.ts:25` `ROLES = ["planner","worker","reviewer"] as const` 原样；注释 `:22-23` 明言「计划生成 / 任务派发 / 审查三条结构化管线只认它们；自定义角色经 RoleRef 联合扩展，不动本联合」。diff 中 `Role` 类型的消费方只在跨层 role 字段处放宽为 `RoleRef`，`ROLE_DEFAULT_ENVELOPES` / `dispatchTask` 等内置管线签名未动 | ✅ |
| 三处读入边界 `isRole → isRoleRef` | `storage/sessions/store.ts:49`、`transcript.ts:152`、`inflight.ts:125`（diff 逐处核实即此三处，别无遗漏——`packages/storage/src` 全域 grep `isRole(\|isRoleRef(` 仅此三处命中） | ✅ |
| **旧数据零迁移独立验证** | 临时单测探针（storage 侧，4 用例，跑完即删）：① 手写 T8.4 之前形状的 sessions.json（role 为 `"planner"`/`"worker"`，含 native 绑定与 resumeKind）→ `listSessions` 不抛、role 原样、`isRoleRef` true / `isRole` true / `isCustomRoleId` false；② 手写旧 transcript.jsonl（turn_end.role 内置字面量）→ `readTranscript` 零 skippedLines、5 条全解析；③ 手写旧在飞标记 → `listInflightMarkers` 零 issues；④ **兼容不等于放水**：role 为 `"architect"`（既非内置也非前缀）仍抛 `sessions-file-invalid`。**4/4 绿** | ✅ |
| 判别矩阵有单测钉住 | `shared/tests/domain.test.ts:433-473`：isCustomRoleId 六种输入（含 `"role-"` 裸前缀 false、数字 false）、内置三字面量与前缀无冲突循环断言、isRoleRef 对旧字面量放行、`"architect"` 拒绝、CustomRole/Profile 绑定形态。探针⑤反证有效（§4-⑤） | ✅ |

### B. 权限红线：§7 危险操作固定清单三重锁（本单最要紧）

**结论先行：三重锁逐层核实为真，且第三层（裁决层）不信任角色预设——手写 roles.json 绕过校验落盘也拦得住。**

| 锁 | 实现 | 逐层核实 | 结论 |
|---|---|---|---|
| 锁 1 · 类型 | `shared/domain/permission.ts:67`：`dangerousOpsRequireApproval: true`——类型固定为字面量 `true`，TS 层无法构造 false 的信封 | 类型层锁死编译期；JSON/IPC 边界的未收窄数据由锁 2 复核 | ✅ |
| 锁 2 · 校验器 | `core/permission/custom-role.ts:79-84`：`!== true` 即违规，reason 引 §7；storage `createRole/updateRole` 经校验回调注入（`roles/store.ts:178-180,194-196`，回调抛错不落盘），`data.ts:160-165` 接线 `validateCustomRoleDraft` → `CustomRoleValidationError` | 运行时探针（临时单测，跑完即删）：构造 `dangerousOpsRequireApproval: false` 的恶意 draft → 校验必拒且违规字段正确。反向探针①：删掉该检查 → `custom-role.test.ts` 恰红 2 条（§4-①）。storage 侧「回调拒绝不落盘」有单测（`roles.test.ts:154-174`，同步 + 异步） | ✅ |
| 锁 3 · 裁决层 | **裁决层不读预设的 dangerousOpsRequireApproval 字段**：`core/permission/command.ts:348-360` 危险规则恒扫描（`BUILTIN_DANGEROUS_COMMAND_RULES` 冻结、只能 extraRules 追加不能移除，`:45-48` 注释明言）、`:371-373` 命中即 `needs_approval` 且**白名单批准不豁免**（`:318` 注释）；`run-guard/judge.ts:101-116` 越 writePaths 删除 → `dangerous_operation` 送审、`:69-80` 项目外路径恒 violation 无审批通道；`envelope.ts:125,133` `intersectEnvelopes` 恒置 true、`:225-229` `applyRunGrant(dangerous_operation)` 直接抛错（批准不落信封） | 运行时探针（临时单测）：把恶意预设（approval:false + shell allowed + writePaths `**`）**未经交集直接**喂给裁决层——`git push` / `npm publish` / `winget install` 全部 `needs_approval` 且危险类别非空；项目外写路径 violation；越 writePaths 删除按危险送审。**4/4 绿——即使锁 1 锁 2 全被绕过（手写 roles.json），裁决层无字段可读、无通道可豁免** | ✅ |
| 信封 = 角色预设 ∩ Profile 预设（T7.2 Reviewer 同款） | `orchestrator.ts:825-829`：`roleDefault = customRole.permissionPreset ?? PLANNER_DEFAULT_ENVELOPE` → `intersectEnvelopes(roleDefault, profile.permissionPreset)`——与 Reviewer 分支 `:747-749` `intersectEnvelopes(REVIEWER_DEFAULT_ENVELOPE, profile.permissionPreset)` 同款直接相交，不走 `assembleRunEnvelope`（讨论轮无任务合同，注释 `:822-824` 讲明）。交集实现 `envelope.ts:116-137` 逐维取窄（路径 intersectScopeLists / shell 最严者胜 / 网络 AND） | 编排器单测「信封交集生效：shell=forbidden 命令判违规掐断整轮」+「在飞表 writePaths 为交集产物 docs」（`session-orchestrator.test.ts` T8.4 组）。交集公式本身探针②反证（§4-②） | ✅ |
| 路径越根升格显式违规 | `custom-role.ts:45-60`：readPaths / writePaths 逐条 `parsePathScope` null → 违规（reason 明言绝对路径 / ~ / `..` 攀升超出上限）。单测 5 形态参数化 + 每条各报 + 项目内不误报（`custom-role.test.ts:100-128`） | ✅ |

### C. 存储（`storage/roles/`）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 镜像 profiles 款式 | `roles/store.ts`：整文件 `{version, roles[]}` + `writeJsonAtomic` 原子写（`:145-148`）+ `readJson` 安全读、not-found 空集（`:110-113`）、损坏隔离上抛 `StorageCorruptJsonError`、结构不符抛 `RolesFileInvalidError`（顶层 / version / 数组 / **id 前缀读入复核** `:133-140`）；id 生成 `role-` 前缀 + 12 位十六进制 + 查重（`:99-105`），与 shared `CUSTOM_ROLE_ID_PREFIX` 同源 import（`:26`，不是手抄字符串） | ✅ |
| 拒删口径 | `errors.ts:49-58` `RoleInUseError`（code `role-in-use`，注释与 Provider 删除保护对照）；`store.ts:213-225` deleteRole：not-found 先查 → `isInUse` 判定被引用即抛、条目保留；`profileReferencesRole`（`:89-94`）纯函数供宿主组装钩子 | ✅ |
| 引用检查在 remove 时必然执行 | storage 层 `isInUse` 是**可选参数**（与校验回调同款 DI 设计——storage 不读 profiles.json 的解耦代价）；「必然执行」由**唯一生产调用点**保证：`data.ts:399-405` `roles:remove` handler 恒传 `profileReferencesRole(await profiles.listProfiles(), roleId)` 钩子，别无第二个 deleteRole 生产调用方（全仓 grep 核实）。这与 Provider 的 `deleteProvider`（`data.ts:324-329` 恒传钩子）**完全同款**——口径核实通过，如实登记见 §2-2 | ✅ |
| 单测覆盖 | `roles.test.ts` 12 条：空集 / 建档形状 / 损坏隔离 / 结构不符 4 形态含 id 前缀复核 / CRUD round-trip 时间戳 / not-found 两径 / 保序 / 校验回调拒绝不落盘（同步+异步）/ profileReferencesRole 三态 / 拒删条目保留 / 解绑后可删（异步钩子）。全部 mkdtemp 真实读写。探针④反证拒删（§4-④） | ✅ |

### D. Prompt 第 1 层

| 核查点 | 证据 | 结论 |
|---|---|---|
| `resolveRoleDefinition` 双径 | `core/prompt/roles.ts:34-43`：内置 `isRole` → 查 `ROLE_DEFINITIONS`；自定义 → `customDefinition` 去空白原文；**缺定义 / 全空白抛错**（`:39-41`，注释「宁可当场失败」把住装配边界）——非静默空提示词 | ✅ |
| 内置逐字不变 | ① `ROLE_DEFINITIONS` 常量文本本单 **diff 零改动**（`git diff ac3cd2d 42f1da5 -- packages/core/src/prompt/roles.ts` 逐行核实：常量体只字未动，改动仅头注 + 新增函数）；② 钉住单测：`prompt.test.ts`「resolveRoleDefinition：内置角色逐字返回 ROLE_DEFINITIONS」（三字面量逐个 `toBe` 引用同一常量）+「内置角色行为逐字不变：worker 组装含 ROLE_DEFINITIONS.worker 原文」（assemblePrompt 全链路 `toContain`）。**强度评估**：这两条钉的是「解析路径逐字查表」（探针③证实改掉查表路径恰红，§4-③），钉不住「常量文本本身被改」（改常量则单测跟着常量变、仍绿）——后者只能靠 diff 审查兜底，本单已兜（①），如实登记 §2-4 | ✅ |
| systemPrompt 原文进第 1 层 | `assemble.ts:91` `# 角色\n${resolveRoleDefinition(params.role, params.customRoleDefinition)}`；编排器 `orchestrator.ts:793-800` 讨论轮条件展开 `customRoleDefinition: customRole.systemPrompt`。单测「assemblePrompt：自定义角色第 1 层为其提示词原文」+ 编排器侧 prompt `toContain` 原文且 `not.toContain("你是规划者")` | ✅ |
| 记忆注入 planner 同款 | `inject.ts:31-39`：`!isRole(role)` → decision + rule（注释三行讲明为何不是 worker/reviewer 策略）。单测「自定义角色（T8.4）：planner 同款」 | ✅ |

### E. 编排器

| 核查点 | 证据 | 结论 |
|---|---|---|
| 角色解析落点 | `orchestrator.ts:615-639`（start 受理内）：worker-task → worker；reviewer-review → reviewer；**仅 `planner-message` + `isCustomRoleId(profile.defaultRole)`** 走自定义分支（loadCustomRole 查询）；其余恒 planner。`loadCustomRole` 为可选依赖（`:199-206`，缺省 = 一切 CustomRoleId 视为不存在），`session/index.ts:182` 接线 `roles.getRole` | ✅ |
| 角色不存在拒受理不 spawn | `:630-633`：查无（含未注入）→ `{accepted:false, reason: "自定义角色不存在：<id>"}` 直接 return——在信封装配 / dispatchTask / spawn 之前。单测双分支（接了查无 + 完全未注入）断言 `captured.length === 0`（未 spawn） | ✅ |
| **planner-plan 恒按 planner** | `:627` 条件明确限定 `kind === "planner-message"`，planner-plan 落入 else `role = "planner"`（注释 `:619-622`：结构化输出合同只属于 planner）；渲染层 `SessionPage.tsx` onGeneratePlan **不传 localRole**（注释与编排器同文）。单测「计划生成轮恒按 planner：第 1 层仍是内置 planner、started.role=planner」。**与主管理员裁定的现状核实一致**：静默按 planner 执行；「生成计划」按钮旁**尚无**提示文案——与裁定「文案补充归下一个文档口径/小工单」相符，本单不构成缺口（§6.2 登记落档项归属） | ✅ |
| 事件与登记 role 为角色 ID | started 事件 / 会话登记 / turn_end / 在飞表全为 CustomRoleId：单测逐项 `toMatchObject`（含 `listActiveTurns` writePaths 为交集产物 `["docs"]`）；契约 `contracts.ts:816` started.role 放宽 `RoleRef` | ✅ |
| 内置行为不受注入影响 | 单测「planner Profile 的讨论轮不受 loadCustomRole 注入影响」（started.role=planner + 第 1 层内置文本） | ✅ |

### F. 渲染层

| 核查点 | 证据 | 结论 |
|---|---|---|
| 设置页角色管理照款式 | `pages/settings/roles/`：`RolesSection.tsx`（列表 / 新建 / 编辑 / 删除，**三态齐全**——loading 骨架 `:82` / error 重试 `:84-91` / 空态一句话 + 动作按钮 `:93-99`，§1.5-2）+ `RoleEditorDialog.tsx`（名称 + 提示词 + `PermissionEnvelopeEditor` 预设复用组件；保存错误对话框内呈现 `:115-119`；canSave 前端粗校验 + 权威在 core）+ `role-form.ts` 纯逻辑（3 单测）。挂载 `SettingsPage.tsx:34`（Profiles 与默认权限之间）。拒删 toast 原因原文呈现（`RolesSection.tsx:56-59`） | ✅ |
| `useRoleLabel` 三径 | `hooks/useRoleLabel.ts:22-30`：内置 `t(session.role.*)` 语言包；自定义经 `roles:list` 查显示名；**查不到回退原始 ID**（被删角色历史记录仍可读，注释引 §1.4 红线 3）。四处消费方统一改经本 hook：状态条 `SessionStatusBar.tsx:44`、恢复列表 `SessionResumePanel.tsx:65`、任务页在飞区 `ActiveTurnsSection.tsx:33`、Profile 徽标 `ProfilesSection.tsx:123`（diff 逐处核实，原 `t(\`session.role.${role}\`)` 硬模板全部移除） | ✅ |
| Profile 角色下拉 optgroup | `ProfileEditorDialog.tsx:197-205`：内置三项之外 `<optgroup label={t("settings.roles.title")}>` 列自定义角色（label 走 i18n，选项文本是用户数据）；`profile-form.ts` defaultRole 放宽 string（select 受控值），边界收窄 `as RoleRef` 一次、权威在 core（注释讲明）。round-trip 单测 +1 | ✅ |
| 会话页讨论 Profile 可选 | `useDiscussionProfiles`（`useRoleProfile.ts:37-42`）：planner 或自定义角色 Profile；`SessionPage.tsx` 多于一个才给下拉、在飞禁切换（`disabled={busy}`）、缺省优先第一个 planner Profile（与 T8.4 前行为一致）；`localRole` 本地回显（`session-run.ts:39-44`，注释明言权威值仍是 started.role） | ✅ |
| locales 键集合 | **实测 zh/en 各 +17 键**（脚本对比 ac3cd2d：`settings.roles.*` 16 + `session.discussionProfile` 1），zh/en 新增集合逐键相等、**全量 parity true**——交付报告称「各 +22 键」数字不准，如实登记 §2-1（键集合一致性与功能不受影响）。`check-i18n` PASS（无硬编码 CJK）、`locales-parity` 单测在 95 文件全绿内 | ⚠️ 数字勘误 |

### G. E2E `custom-role.spec.ts`（断言强度逐条评）

1. **建角色**（`:28-33`）：走真实表单（`#role-name` / `#role-prompt`）→ Save → 行可见。中——只断言名称可见，预设走默认值（写路径空 / shell forbidden，天然合法），未覆盖校验拒绝分支（core 侧 14 单测已钉，E2E 不重复合理）。
2. **Profile 绑定经 optgroup**（`:52-59`）：`selectOption({ label: roleName })` 命中自定义角色选项——optgroup 下拉可选的呈现层实证。**较强**。
3. **徽标解析**（`:62-63`）：`toHaveCount(2)`——页面恰两处角色名（Profile 行徽标 + 角色区行），是 `useRoleLabel` 经 roles:list 解析生效的实证（若徽标显示原始 ID 则 count=1）。**强**。
4. **被引用拒删**（`:66-68`）：删除按钮 → toast `Failed to delete custom role` 可见 + 行保留。呈现层强度够；「roles.json 条目仍在」由 storage 单测「拒删条目保留」钉住，分层合理。
5. **解绑后可删**（`:71-74`）：删 Profile → 隐藏 → 删角色 → 隐藏。**强**（完整闭环）。

**hermetic 合理性**：spec 头注明言只走设置页表单与 roles:*/profiles:* IPC、不跑真机 Agent 轮（「自定义角色跑真实会话属验收演示」）——与工单裁定 1 一致，且本次验收已真机补上（§5）。链路除无 Agent 轮外全真实（表单 → IPC → core 校验 → roles.json/profiles.json 落盘 → 列表回读）。一处现状说明如实（`:47-48` reload 是 Profile 编辑器查询挂载时发起的既有行为，非被测对象）。**评估：强度达标**。

### H. 红线（`git diff ac3cd2d 42f1da5 --stat`，51 文件 +2129/−98）

| 红线 | 证据 | 结论 |
|---|---|---|
| fixtures / 依赖 / lock / 适配器零改动 | `--stat -- 'package.json' 'pnpm-lock.yaml' '**/package.json' 'packages/adapters' 'fixtures' '**/fixtures/**'` **零输出** | ✅ |
| 51 文件与交付报告一致 | stat 清单逐一对照交付报告落点段：领域 3 + core 7（含 tests 3）+ storage 8（含 tests 1）+ desktop main 3 + 契约 1 + renderer 17 + desktop tests 5 + locales 2 + 进度文档 1 = 51，完全吻合 | ✅ |
| 半成品死代码抽查（续用评估是否靠谱） | 新增导出逐一 grep 消费方：`ROLES_FILE_VERSION`/`RolesFile`/`RoleStoreError` 族 / `RoleDraftValidator`/`RoleInUseCheck`（data.ts + tests）、`CustomRoleDraftWire`（role-form + contracts）、`profileReferencesRole`（data.ts × 2 处 + tests）、`resolveRoleDefinition`/`validateCustomRoleDraft`/`CustomRoleValidationError`（assemble/data.ts + tests）、`isCustomRoleId`/`CUSTOM_ROLE_ID_PREFIX`（orchestrator/validate/store/hooks + tests）、`useRoleLabel`（4 消费方）、`useDiscussionProfiles`（SessionPage）——**无一悬空**。半成品部分（领域 / 校验器 / store / 契约 / data.ts / Prompt 第 1 层）全部接线到位，「续用并补完」的评估靠谱 | ✅ |
| §1.4 非 MVP 红线 | 新增文件与 diff 通读：无 TODO / 占位；strict TS 可选字段条件展开（`customRoleDefinition` `:798`、`loadCustomRole`、`getCustomRole`、`localRole`、`isInUse` 等）；新逻辑全带单测（+47：core 24 + storage 12 + shared 1 + desktop 10）；未 push（`main [ahead 12]`） | ✅ |

---

## 2. 抽查发现

**未发现功能缺陷，未发现红线被绕开。** 以下如实登记（均不影响结论）：

1. **（交付报告数字勘误，不影响结论）locales 增量实为 zh/en 各 +17 键，非报告所称 +22。** 脚本对比 ac3cd2d 与 HEAD 的叶子键集合：新增 `settings.roles.*` 16 键 + `session.discussionProfile` 1 键 = 17，zh/en 新增集合逐键相等、全量 parity 为 true。keys 一致性（真正的红线）无恙，纯计数错误，建议进度文档落档时顺手勘误。
2. **（口径核实，维持现状合理）storage `deleteRole` 的 `isInUse` 是可选参数**——「引用检查必然执行」不由 storage 层强制，由唯一生产调用点 `data.ts:399-405` 恒传钩子保证（与 Provider `deleteProvider` 完全同款既有设计）。全仓 grep 证实无第二个生产调用方。若未来新增 deleteRole 调用点须自觉带钩子——与 Provider 侧同一风险面，不单独立项。
3. **（正面观察）读入边界「兼容不放水」**：`isRoleRef` 放行旧字面量的同时，非法 role（`"architect"`）在 sessions 读入仍确定性抛错（A 项探针④用例），transcript 侧坏行跳过计数——两种边界处置各自沿用其层的既有语义，未因放宽联合而松动。
4. **（单测强度边界，如实登记）「内置逐字不变」单测钉的是解析路径而非常量文本本身。** 两条单测均 `toBe`/`toContain` 引用 `ROLE_DEFINITIONS` 常量——若有人改常量文本，单测跟着常量变、仍绿；它们能红的场景是「内置角色也走自定义分支 / 查表被绕开」（探针③证实恰红）。「常量文本不变」只能靠 diff 审查兜底，本单已核实零改动。这是此类快照钉法的固有边界，非本单缺口。
5. **（演示环境观察，不影响结论）本机 codex headless 不可用**：`codex login status` 报已登录，但 `codex exec` 两次挂起 3.5 分钟以上零输出（缺省模型与 `-m gpt-5` 各一次，杀树收场）。属本机环境问题（E2E 的 codex 替身路径不受影响），真机演示改走 claude-code 完成。若续验清单有 codex 真机项，此观察供参考。
6. **（E2E 断言分寸，不影响结论）custom-role.spec 第 1 步只断言角色名可见**，未在 E2E 层覆盖校验拒绝分支（空名 / 越根路径 → 对话框内报错）。core 侧 14 条校验单测已钉住全部拒绝路径，E2E 不重复是合理分层；若未来做表单交互回归可顺手补一条，不单独立项。

---

## 3. 旧数据兼容独立验证与真机演示（工单 A 项 + 裁定 1 顺带项）

### 3.1 旧数据零迁移（临时单测探针，4/4 绿，跑完即删）

见 §1-A 表内明细。要点：T8.4 之前形状的 sessions.json / transcript.jsonl / 在飞标记（role 全为内置字面量）经当前读取路径**不抛、不丢、类型判定正确**；且非法 role 仍被拒——「零迁移」的声称独立核实为真。

### 3.2 真机演示（claude-code，全链路真实）

临时脚本（沿 live-ten-step.mjs 款式，用完即删）驱动生产 IPC 全栈：

| 步骤 | 结果 |
|---|---|
| roles:create（core 校验 + roles.json 落盘） | `role-63982b341a17`「验收演示·文档顾问」，只读预设（writePaths 空 / shell forbidden） |
| cli_login Provider + claude-code Profile 绑定该角色 | `profiles:create` 经 `validateProfileDraft`（getCustomRole 接线）通过 |
| planner-message 讨论轮（真机 claude，登录态自管） | **end reason = completed**；**started.role = role-63982b341a17**（自定义角色 ID，非 planner）；answer 非空且内容切题（README 应包含什么，中文答复符合输出语言缺省）；事件序 started → text → end |
| 会话登记复核 | `sessions:list` 首条 role = role-63982b341a17 |

**合同验收判据「自定义角色建 Profile 跑通一轮真实会话，权限预设生效」成立**：会话真实跑通、按角色行事；权限预设生效的运行时证据由编排器单测「shell=forbidden 命令判违规掐断整轮」+ 交集信封登记（§1-B/E）承担，演示轮为纯文本讨论（未触发权限事件，符合只读角色预期）。演示结束脚本已删、临时目录已清、`git status --short` 干净。

---

## 4. 反向验证（5 条探针，全部编辑器工具改 → 只跑对应文件 → 观察 → `git checkout` 还原 → 涉 dist 者重建 → 回绿）

| # | 探针 | 结果 |
|---|---|---|
| ① | 校验器删掉 `dangerousOpsRequireApproval !== true` 检查（放行危险操作关闭） | `custom-role.test.ts` **2 failed / 12 passed**——恰为「§7 红线：危险操作确认不可关闭」+「多处违规一次收齐」（少一条违规）。锁 2 被钉死 |
| ② | `intersectShellPolicies` 交集改并集（最宽者胜） | `permission.test.ts` **3 failed / 63 passed**（shell 交集三态矩阵红）。**追加观察**：重建 core 后 desktop `session-orchestrator.test.ts` **77/77 未红**——编排器 T8.4 组的「shell=forbidden 掐断整轮」用例中角色预设与 Profile 预设 shell 同为 forbidden，并集仍 forbidden，感知不到该变体。交集公式由 core 层钉住（3 条红），编排器层是消费方不重复钉，**非缺口**；如实登记供参考 |
| ③ | `resolveRoleDefinition` 内置角色也走「加工」分支（文本追加后缀） | `prompt.test.ts` **1 failed / 18 passed**——恰为「内置角色逐字返回 ROLE_DEFINITIONS」。逐字查表被钉死 |
| ④ | `deleteRole` 跳过 `isInUse` 钩子判定 | `roles.test.ts` **1 failed / 11 passed**——恰为「isInUse 判定被引用：抛 RoleInUseError 且条目保留」。拒删被钉死 |
| ⑤ | `isRoleRef` 拒绝内置字面量（破坏旧数据兼容） | `domain.test.ts` **1 failed / 59 passed**（判别矩阵）；重建 shared 后 storage `sessions.test.ts + transcripts.test.ts` **8 failed**（读入路径连锁红）。旧数据兼容被 shared + storage 两层钉死 |

**5 条全部恰红于目标用例**（满足「至少 4 条」）。每条还原后 `git status --short` 为空（无 CRLF 残留）；②⑤ 还原后重建 shared/core；基线复跑 8 文件 **267/267 回绿**。

---

## 5. 自动化回归结果

全部在仓库根执行，HEAD = `42f1da5`。

| 项 | 结果 |
|---|---|
| 单测第 1 次 | **1911 / 1911（95 文件）**，exit 0，17.14 s |
| 单测第 2 次 | **1911 / 1911（95 文件）**，exit 0，16.89 s |
| 单测第 3 次 | **1911 / 1911（95 文件）**，exit 0，17.63 s |
| `pnpm lint` | exit 0 —— biome `Checked 578 files. No fixes applied.` + `[check-i18n] PASS`（renderer 无硬编码 CJK） |
| `pnpm build` | exit 0 —— 6 工程含三套 typecheck，renderer 产物 1,546.91 kB |
| `pnpm smoke` | exit 0 —— 七项全 PASS（main-sqlite · ipc-ping-pong · app-info · sqlite-via-ipc · event-subscription · csp-blocks-eval · secrets-roundtrip），**`No handler registered` 0 行**（Select-String 计数独立核实 = 0） |
| `pnpm test:e2e` 第 1 次 | **26 passed / 1 failed**——失败为 `task-parallel` 第 3 条（§4.5 已知 flake，全程 2.7 min 负载性超时表现，与 T8.3b 验收 §2-6 定性同款） |
| task-parallel 单 spec 复跑 | **3/3 绿**（2.4 s）——按既有口径复跑绿即登记 |
| `pnpm test:e2e` 第 2 次 | **27 passed（18.4 s）**，exit 0（含新增 custom-role 1 条） |

已知 flake（adapters `generic-exec` cancel）三连跑均未触发；E2E task-parallel 第 3 条触发一次、复跑绿，按 §4.5 既有登记处置，无需新增登记。

验收结束时 `git status --short`：

```
?? docs/验收记录/T8.4-验收.md
```

---

## 6. 交付方裁定请求的主管理员结论（如实登记；落档动作归下一单）

1. **「真实会话跑通一轮」归验收环节手工执行**——本次验收已顺带执行（本机 claude-code 登录态可用），结果 PASS（§3.2），**无需进续验清单**。本机 codex headless 挂起的环境观察见 §2-5。
2. **planner-plan 恒按 planner + 按钮旁提示文案**——现状实现与交付报告一致（§1-E 核实：静默按 planner 执行、onGeneratePlan 不传 localRole、按钮旁尚无提示文案）。文案补充归下一个文档口径 / 小工单，本验收只核实现状，现状与裁定预期吻合。
3. **会话页下拉行为已知悉**——无动作。现状核实：多于一个讨论 Profile 才显示下拉、在飞禁切换、缺省优先 planner Profile（§1-F）。

---

## 7. 遗留 / 建议登记的技术债

| 项 | 说明 | 建议归属 |
|---|---|---|
| 交付报告 locales「+22 键」数字不准（实为 +17） | §2-1；keys parity 无恙 | 进度文档落档时顺手勘误，不立项 |
| planner-plan 按钮旁提示文案 | 主管理员已裁定方向（维持静默 planner + 文案说明），本单核实现状无文案 | 归下一个文档口径 / 小工单（裁定既定归属） |
| 「内置逐字不变」单测钉不住常量文本本身 | §2-4：快照钉法固有边界，改常量仍绿；靠 diff 审查兜底 | 备查，无需处置 |
| 本机 codex headless 挂起 | §2-5：login status 正常但 exec 无输出；不影响仓内任何自动化 | 环境观察备查；codex 真机相关续验项执行前先复测 |

---

## 8. 验收结论

### **通过。**

- **合同判据成立**：「自定义角色建 Profile 跑通一轮真实会话，权限预设生效」——真机演示 PASS（claude-code 全链路：roles:create → Profile 绑定 → planner-message 轮 completed，started.role / 会话登记均为自定义角色 ID，§3.2）；权限预设生效由编排器单测（forbidden shell 掐断整轮 + 在飞表交集 writePaths）与三重锁核实（§1-B）共同证实。
- **B 项三重锁逐层核实为真**：类型层字面量 true 锁死编译期；校验器复核 JSON/IPC 边界（放开即拒，探针①恰红）；**裁决层不读预设任何字段**——恶意 roles.json 直接落盘的场景下 classifyCommand / judgeFileChange 仍拦（运行时探针 4/4 绿），`applyRunGrant(dangerous_operation)` 抛错、批准不落信封。信封 = 角色预设 ∩ Profile 预设与 T7.2 Reviewer 同款直接相交，路径越根升格显式违规。**§7 固定清单物理不可绕过的声称成立**。
- **A 项旧数据零迁移独立核实为真**：旧形状 sessions.json / transcript.jsonl / 在飞标记读入不抛、类型判定正确、非法 role 仍拒（临时探针 4/4 绿）；`Role` 闭合三字面量未被打开。
- **反向验证**：5 条探针全部恰红于目标用例（①②③④⑤），还原后基线 267/267 回绿、status 干净。
- **红线未破**：51 文件与交付报告一致；fixtures / 依赖 / lock / 适配器零改动；无 TODO / 占位；半成品续用部分无死代码（新增导出全有消费方）。
- **回归数字**：1911/1911 × 3（95 文件）· lint exit 0 · build exit 0 · smoke 七项 PASS 且 `No handler registered` 零行 · E2E 27/27（第 1 跑 task-parallel 已知 flake 触发一次，单 spec 复跑 3/3 绿 + 全量复跑 27/27，按 §4.5 既有登记处置）。
- **「通过」不带遗留标记的理由**：§2 六条无一为功能缺陷（1 报告数字勘误、1 口径核实、2 正面/边界观察、1 环境观察、1 分层合理性说明）；§7 四项均为备查/勘误/既定归属，无新技术债。

**需主管理员裁定事项：**

1. §2-1 locales 键数勘误（+22 → +17）随 T8.4 落档提交顺手修正进度文档——是否照办；
2. 裁定 1 的「真实会话」已由本验收执行完毕（claude-code），续验清单**无需**新增该项——请确认此口径；
3. §2-5 本机 codex headless 挂起是否需要在续验清单相关项上加注「执行前先复测环境」。


