# T8.7 验收记录（记忆语义检索）

**执行时间：** 2026-09-06
**执行人：** 验收子 Agent（独立验收，边验边写）
**被验提交：** `b9f3d69`（`feat: T8.7 记忆语义检索 —— 记忆向量索引 + 嵌入管道接入 + FTS/向量 RRF 融合（Phase 8 收官，Phase 6 向量路真机保留项闭环）`，HEAD）
**依据：** `docs/开发进度.md` §0「T8.7 ✅ 完成（待验收）」（交付报告）与 §4.5 新行；`docs/开发计划.md` §16.4 T8.7 行（合同：复用既有两层**不另写一套**、schema 迁移照 T6.4 款式、顺带补验 Phase 6 向量路真机）与 §1.4；`docs/验收记录/Phase6-验收.md` §3（保留项原文）；`docs/验收记录/T8.6b-验收.md`（最新验收款式）
**版本关口：** Phase 8（M3）最后一个工单；通过后进入 Phase 8 关口收尾（tag `v0.9.0` 归关口收尾单）

---

## 0. 执行环境

| 项 | 值 |
|---|---|
| 平台 | Windows 10（10.0.22631），PowerShell，全部命令在仓库根执行 |
| 包管理器 / Node | pnpm 11 · Node 24 · Electron monorepo（vitest，biome） |
| 被验 HEAD | `b9f3d69`。开工时 `git status --short` 干净（无其他 Agent 并行） |
| 真机 | 本机 Ollama 0.33.3 运行中（`http://127.0.0.1:11434`），`bge-m3` 已拉取——探活实测 `/v1/embeddings` 返回 1024 维。两条 live 脚本均真机实跑 |
| 跨包重建 | desktop 单测消费各包 dist；live 脚本实跑前 `pnpm build` 全量重建（exit 0）。storage 侧探针经 vitest 直测 TS 源无需重建；desktop 侧探针改 desktop 自身源码也无需重建 |
| 已知 flake（§4.5 名单七条） | adapters generic-exec cancel · E2E task-parallel · vitest worker fork · opencode env 指纹 · iflow `--experimental-acp` 漂移 · grok-build respondPermission 超时 · **process 树杀前置探活（本单新登记）**——触发则复跑绿即登记 |

---

## 1. 逐条核验（读码为证，不采信交付报告自证）

### A. 「复用不另写一套」合同核查（本单最要紧）

| 核查点 | 证据 | 结论 |
|---|---|---|
| rag 包 src 零改动 | `git diff 5c1e648 b9f3d69 --stat -- packages/rag/` **零输出**（含 src 与 tests，一行都没动） | ✅ |
| RRF 经既有出口导入，无第二份实现 | `memory-search.ts:21` `import { fuseByRrf, type RankedList } from "@ff-pane/rag/retrieve"`（与 `knowledge-search.ts:31` 同一导入口，:19-20 注释说明走子路径不走 barrel 的理由与 knowledge 侧一致）；全仓 grep `function fuseByRrf` 恰一处 `packages/rag/src/retrieve/rrf.ts:60`、`function rrfScore` 恰一处 `packages/rag/src/index.ts:20`——**无任何复制实现** | ✅ |
| 嵌入管道复用 | desktop `main/memory-index.ts:27-28` `import { embedChunks } from "@ff-pane/rag"`、`:51` 复用知识库的 `resolveKnowledgeEmbedder`（同一解析器、同一 Provider 配置面，不新造配置项） | ✅ |
| 表名参数化非复制 | `knowledge-vector.ts:34-45` 新增 `VectorTableSpec` 接口 + `:48-54` `KNOWLEDGE_VECTOR_TABLES` 缺省常量；逐 diff 核实：`createVec0Index`/`createFallbackIndex`/`readVectorState`/`createVectorTable`/`ensureVectorIndex`/`openVectorIndex`/`dropVectorIndex` 全部加 `tables: VectorTableSpec = KNOWLEDGE_VECTOR_TABLES` 参数、表名常量替换为 `tables.*`，**算法逻辑零改动**（vec0 三坑防线——rowid BigInt `:164-169`、虚表无 UPSERT 先删再插 `:223-230`、单元素 IN 补双 `:274-283`——全部只存一份且未被触碰） | ✅ |
| 既有调用零改动 | 全部新参数带缺省值 = 知识库表组，`main/knowledge/` 与 `knowledge-search.ts` 的既有调用点零 diff；知识库行为不变由 knowledge 相关单测全绿证实（§4 回归） | ✅ |
| 记忆侧绑定 | `memory-vector.ts:40-46` `MEMORY_VECTOR_TABLES` 绑 `memory_vec0`/`memory_vector`/`memory_vector_state` 三表 + `entry_rowid` 属主列，全部记忆侧函数是对 knowledge-vector 函数的表组转发（`:63-91` 每个都是一行 delegate） | ✅ |

### B. 迁移与数据安全

| 核查点 | 证据 | 结论 |
|---|---|---|
| v3 只建状态表、向量表惰性 | `memory-vector-schema.ts:35-54` DDL 恰两张表（`memory_vector_state` 单行 CHECK(singleton=1) + `memory_embedding_state`）；`:16-18` 注释明示惰性理由与 v2 同款（vec0 维度 CREATE 时固定、取决于用户模型）；对照 `knowledge-schema.ts` 的 v2：同款单行状态表 + 向量表不进迁移——**款式一致**。登记进 `schema.ts:84-86` `INDEX_DB_MIGRATIONS` 末尾（照 T6.4 登记方式） | ✅ |
| v2 旧库升级无损断言强度 | `memory-vector.test.ts:151-167`：真造 v2 停点库（`runMigrations(legacy, INDEX_DB_MIGRATIONS.slice(0, 2))` + 断言版本恰 2）→ 写一条记忆 → 重开升 v3 → 断言①版本落 3 ②既有记忆 FTS 照常命中（`searchMemoryIndex` 查回 `mem-legacy`）③新表就位且为空。断言强度足：不是只查版本号，连数据可查性都钉了 | ✅ |
| FK CASCADE 行为 | `memory-vector-schema.ts:49-50` `entry_rowid REFERENCES memory_entry(entry_rowid) ON DELETE CASCADE`；外键在连接层开启（既有 openIndexDb 纪律）；`:47-48` 注释如实标注 vec0 虚表 CASCADE 管不到、向量删除由 memory-index 原语显式处理——`memory-index.ts:94-99`（delete 显式 deleteMany）与 `:120`（rebuild 显式 clear）确证。单测 `:320-330`（删除连带清记账）+ `:332-342`（rebuild 后 CASCADE 出清记账行）钉死；反向探针④恰红（§3） | ✅ |
| 同事务先向量后记账 | `memory-vector.ts:142-156` `storeMemoryVector`：`db.transaction(() => { index.put(...); INSERT INTO memory_embedding_state ... })`——put 在前、记账在后，同一事务。崩溃语义论证成立：事务原子性下两者要么都提交要么都回滚；**顺序的意义在 put 抛错时**（维度不符 assertDimensions 抛 RangeError）——记账语句尚未执行，事务回滚后零残留，绝不出现「记了账没向量」的假完成；反之若先记账，put 抛错虽也回滚，但顺序防的是「记账成功、put 静默失败」类缺陷面。注意：vec0 的 `index.put` 内部自带一层 `db.transaction`（knowledge-vector.ts:227-230），better-sqlite3 嵌套 transaction 自动转 savepoint，语义无损。**无直接钉「拆开事务」的崩溃单测**（§3 探针③如实登记为观察缺口，见该节评估） | ✅（探针③附带评估） |
| upsert 哈希失配作废旧向量 | `memory-index.ts:55-82` `upsertMemoryEntry`：影子行 upsert 后，有向量索引时读记账哈希（`:70-72`）→ 与当前文本哈希比对（`:76-77`）→ 失配即同事务 `deleteMany + DELETE 记账行`（`:78-79`）。哈希只认文本（`memoryTextHash` 不掺模型——模型在向量状态表整库唯一，`memory-vector.ts:57` 注释论证成立）；status 不进嵌入文本（`memoryEmbeddingText` 只拼 title/body/tags）故状态流转不触发重嵌，单测 `:310-318` 钉死 | ✅ |
| 差额判定三合一 | `memory-vector.ts:109-136` `listMemoryRowsForEmbedding`：LEFT JOIN 记账表、`storedHash !== textHash` 即进差额——状态行缺席（NULL ≠ hash）与哈希失配走同一比较。首次回填 / 断点续传 / 编辑后重嵌入确为同一段判定（交付声称成立） | ✅ |

### C. 检索正确性

| 核查点 | 证据 | 结论 |
|---|---|---|
| 双路召回 + RRF 融合 | `memory-search.ts:384-439` `searchMemoryHybrid`：关键词路（≥3 码点 FTS / 否则 LIKE，`:396-399`）+ 向量路（配了 vectorIndex+queryVector 才加，`:406-416`）→ `fuseByRrf(lists, { limit })`（`:418`）。命中带 `sources`/`ranks`（融合来源如实透出） | ✅ |
| 超额召回 limit×4 | `:236` `MEMORY_RECALL_MULTIPLIER = 4`、`:393` `recallLimit = Math.max(limit * 4, limit)`，两路同用；单测 `:484-510`「深位语义命中被救回」——关键词路第 5 名、向量路第 1 名的条目在 limit=3 下仍进前三，钉死超额召回的意义 | ✅ |
| 过滤前置下推 | `recallByVector`（`:307-353`）：category/status 编译成影子表 WHERE（`:284-300`）→ 候选 rowid 白名单传 `vectorIndex.search({ candidates })`——精确前置（knowledge-search 决策 1 同款）。量级论证成立：记忆百到千条（§8.2.5）恒在 `VECTOR_PREFILTER_MAX_CANDIDATES = 20_000` 绑定上限内，不需要知识库的「超额召回 + 事后过滤」退路。FTS 路过滤经既有 `searchMemoryIndex` 的 IN 条件（两路都生效，desktop 单测 `:280-293` 钉死） | ✅ |
| 未配嵌入来源纯 FTS 一等状态 | `searchMemoryHybrid`：vectorIndex/queryVector 缺席时 lists 只有关键词一路，`fuseByRrf` 单路退化保持关键词顺序（rag 单测既有覆盖）；`usedVector: false` 如实。desktop 侧 `main/memory-index.ts:241-242`：向量路缺席时把 `resolution.status.blocker` 透出为 `embeddingBlocker`（no-provider / spec-mismatch 复用知识库码集，contracts.ts 类型引用 `KnowledgeEmbeddingBlocker`）。单测双钉：storage `:456-464`（纯 FTS 顺序 + sources=["fts"]）、desktop `:249-265`（blocker=no-provider + 语义查询对照组零命中）；探针⑤恰红（§3） | ✅ |
| 陈旧向量防命中空气 | `recallByVector:345-352`：向量命中但影子行已删（钩子漏调）→ 跳过不进结果；融合池 id 读不回影子行同样跳过（`:427-429`）——两层兜底不让索引内部不一致毁掉检索 | ✅ |

### D. 主进程服务（desktop `main/memory-index.ts`）

| 核查点 | 证据 | 结论 |
|---|---|---|
| 差额回填单飞 | `:156-195` `ensureBackfill`：`state.backfill` 在飞即直接返回同一 Promise（`:158-160`）；完成后 `finally` 置回 undefined（`:191-193`）。并发检索共享同一次回填，不重复发嵌入请求。desktop 单测「断点续传：第二次检索不再重嵌」以**服务端请求计数**钉死（mock.embedded 长度恰 +1 查询文本） | ✅ |
| 嵌入失败只降级不抛 | `:218-227`：`ensureBackfill` 与查询嵌入包在同一 try，抛错只 log + 继续走纯关键词（queryVector 保持 undefined）；`embedChunks` 自身的批级失败也只进 report 不抛（`:184-189` 只 log）。单测「嵌入端点故障：检索退化为纯关键词、不抛错」钉死（端点先关再首查） | ✅ |
| 惰性打开 + 进程内缓存生命周期 | `:97` states Map 键按 Windows 大小写不敏感归一（`:88-91`）；缓存的是 Promise，打开失败自摘（`:132-137`，不留半死缓存）；`close()`（`:285-290`）遍历关连接 + 清 Map。**泄漏面评估**：service 在 `data.ts:174-178` 装配为模块级单例，生命周期与主进程等长——项目删除（`projects:remove`）不清对应连接，连接与 `<项目>/.workbench/index.sqlite` 句柄保留到进程退出。量级：每项目一个 SQLite 连接，用户项目数量级为个位到十位，进程退出 OS 全量回收；Windows 下已删项目的 db 文件句柄未关会阻止目录彻底删除——但 `projects:remove` 本就只移除登记不删项目目录（既有语义），故无实际用户可见故障。**如实登记 §5 观察项，不构成缺陷** | ✅（§5 登记） |
| 规格失配自动 drop+回填 | `:205-213`：blocker=spec-mismatch 时 log + `dropMemoryVectorIndex`（连带清记账，drop 后差额=全量）→ 置空句柄 → 重新解析。与知识库手动重建的口径差异见 §5 裁定登记（主管理员已裁定接受：记忆百级秒级重算 vs 知识库万级分钟级，且记忆页无重建按钮——模块注释 `:16-19` 论证已落档） | ✅ |
| 首开对账不是重建 | `:115-119`：openProject 内 `reconcileIndexFromStore`；storage 侧 `memory-sync.ts:93-115`：逐条 upsert + 出清幽灵行——**rowid 稳定**（upsert ON CONFLICT 保持 rowid）、未变条目哈希一致不触发作废、真变了的走 upsertMemoryEntry 哈希判定作废。单测 `:513-556` 四态一测（新增/消失/未变保留/变了作废）钉死 | ✅ |
| 写钩子纪律 | `data.ts`：`memory:update`/`memory:approve` 先 `saveEntry` 成功再 `entrySaved`（`:493-496` 注释明示 W1.3b 纪律）；`memory:reject` 先 `deleteEntry` 且 `removed` 为真才调钩子；`tasks:accept` 派生候选逐条 save 后钩子。钩子内部：项目没开过连接就不开（`:259-263`，首查对账捡起）、失败只 log（`:267-269`） | ✅ |

### E. live 真机实跑（两脚本本验收均实跑，Ollama 0.33.3 + bge-m3 1024 维）

**`live-memory-search.mjs`（新，记忆端到端）——本验收实跑 ALL PASS（exit 0）：**

| 判据 | 实跑结果 |
|---|---|
| 对照组（配 Provider **之前**取）语义查询「怎么跑单测」 | 0 命中 · usedVector=false · blocker=**no-provider** ✅ |
| 混合检索语义召回（工单点名实例） | 「执行 vitest 命令」被「怎么跑单测」命中**排第一**、来源 `[vector]`（328ms 含 3 条回填）✅ |
| 第二语义组 | 「发版要注意什么」→ 第一名 mem-live-deploy（部署流程）✅ |
| 断点续传 | 第二查 41ms（首查 328ms 含回填；正确性由单测请求计数钉，脚本只登记数字——判据设计诚实） |
| 编辑后重嵌入 | 条目改写为「数据库备份策略」后「定期存档数据怎么安排」命中同条 ✅ |
| 关键词照常 + category 过滤下推 + 删除出索引（reject 后语义不再命中） | 全 PASS ✅ |

判据质量评估：对照组在配 Provider 之前取（脚本 :150-151 注释明示「证明后面的语义命中确实是向量带来的」）——语义判据不是「没报错」而是「向量赢过关键词」，与 Phase6-验收 §3 的判据设计一脉相承。语料三条与查询零字面重合（trigram ≥3 码点意义上）核实属实。

**`live-embedding.mjs`（Phase 6 保留项闭环复验）——本验收实跑 ALL PASS（exit 0）：**

| 判据 | 实跑结果 |
|---|---|
| 百级导入 | 437 文件 → **2289 块** / 1.96s / 零失败；增量重导 437 全跳过 0.09s（交付报告为 2282 块——此后文档又增长，与 Phase6-验收 §2.1 登记的同类口径差异同性质） |
| 补齐向量（增量 import） | 89.4s / embedded=2289 / embedFailed=0 / **indexed=0**（修正后新增的断言：补齐轮不该重索引任何文件）/ 后端 vec0 / 1024 维 |
| 语义判据 | 「怎么把一堆资料喂给模型让它自己去翻」纯 FTS 对照组 **4 命中** → 混合 **5 命中且 3 条含 vector 路**、vectorPrefilterExact=true（交付时对照组 2 命中；文档增长使对照组变肥，但「向量带进对照组没有的命中」的判据仍成立——vector 路独有命中 fetch-models 源码等 2 条是纯 FTS 摸不到的） |
| 关键词回归 | 「RRF 融合」5 命中、top3 双路命中 ✅ |
| 断点续传 | 续传轮 **embedded=0 / embedSkipped=2289**（修正后新增断言 embedSkipped 非零同时钉住）✅ |

**续传判据修正的独立评估（裁定请求 1 的核实部分）：** 修正**合理**。读 `main/knowledge/index.ts:347-372` 核实：`knowledge:rebuild` handler 恒传 `force: true` 进 ingest；`ingest.ts:179` 的跳过条件 `!request.force` 在 force 下失效 → 每个文件都走 `replaceEntryChunks` 整条换新块、rowid 全部换新、旧向量随旧块删除——**rebuild 之后向量必然全量重算**，原脚本用 rebuild 验续传在真实嵌入器上恒失败，交付方论证与代码事实一致。改为增量 import（`force: false` 缺省 → 内容未变跳过重索引、嵌入按 existingRowids 只补差额）确是「断点续传与补齐向量同一段代码」的正确入口（T6.5 语义）。且修正是**加严**不是放宽：新增 `indexed !== 0` 与 `embedSkipped === 0` 两条 findings 断言。`git diff` 核实该文件改动全部在脚本判据层，**产品代码零改动**属实。此前未暴露的原因（脚本从未真机跑过——Phase 6 验收时三条嵌入来源全落空）与 Phase6-验收 §3 记录吻合。**Phase6-验收.md 勘误注记归关口收尾单，本单不改。**

### F. UI 与契约

| 核查点 | 证据 | 结论 |
|---|---|---|
| `memory:search` 契约与 handler 一致 | `contracts.ts:356-393` Request（projectRoot+query+categories/statuses/limit）与 Response（hits/usedFts/usedVector/embeddingBlocker?）；`:1030-1034` IpcInvokeContracts 登记；`:1192` INVOKE_CHANNELS 登记（preload 放行面）；`data.ts:519` handler `(request) => memoryIndex.search(request)` 直通、DataChannel 联合 `:116` 补 `"memory:search"`。service 的返回值构造（`main/memory-index.ts:243-255`）字段与 Response 逐项对齐（exactOptionalPropertyTypes 条件展开）。`contracts.test.ts:64-66` 通道登记单测 | ✅ |
| 250ms 防抖 | `MemoryPage.tsx` `SEARCH_DEBOUNCE_MS = 250` + `useMemorySearch` hook：setTimeout 防抖 + `requestSeq` ref 防乱序回包（旧响应晚到不覆盖新查询）+ 清空即回 undefined。检索失败静默回退不弹 toast（hook 注释明示决策） | ✅ |
| 三态回退纯函数 | `memory-view.ts` `applyMemorySearch`：空查询原样返回 / ids=undefined（在飞/失败）回退本地子串过滤 / ids 可用时**按命中顺序**返回（保留 RRF 融合排序，注释明示理由）+ 命中引用本地没有的 id 静默跳过。`memory-view.test.ts` +4 恰钉这四态 | ✅ |
| 零新 i18n 键 | locales diff 恰两行：zh/en 各改 `memory.searchPlaceholder` 一个**既有键**的值（同步改），零新键——口径属实 | ✅ |

### G. 红线

| 核查点 | 证据 | 结论 |
|---|---|---|
| 23 文件与交付报告一致 | `git diff 5c1e648 b9f3d69 --stat` 恰 23 文件（+2440/−69），清单与交付报告落点节逐一对上 | ✅ |
| 依赖 / lock 零改动 | diff 过滤 `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml`（含各包 package.json）**零输出** | ✅ |
| fixtures 逐字节不动 | `git diff --stat -- packages/adapters/fixtures/` **零输出** | ✅ |
| rag src 零改动 · 适配器零改动 | `-- packages/rag/` 与 `-- packages/adapters/` 均**零输出** | ✅ |
| `knowledge-index.test.ts` 断言放宽合理性 | `=2` → `>=2` 恰一处（`:135-142`），注释明示「对齐迁移清单长度而不是写死 2：T8.7 追加了 v3，本断言只关心 v2 已执行」。**合理**：该用例主旨是「知识库表齐备」，版本号写死 2 在迁移清单加长后必红，放宽为 ≥2 不损失该用例的防护面——「恰为 3」由 `memory-vector.test.ts:138-149` 新用例接管（`readUserVersion(db)).toBe(3)` 精确钉死），版本精确性防护未出现空窗 | ✅ |
| 零 TODO / strict TS | 新文件 grep `TODO/FIXME/XXX` 零命中；exactOptionalPropertyTypes 条件展开款式贯穿（memory-search.ts `:179-181`、main/memory-index.ts `:232-237` 等）；未 push（`main [ahead 1]`） | ✅ |

### H. habits 不顺带的前提核实

grep 证实：`matchesHabitSearch` 仅存在于渲染层（`habits-view.ts` + `HabitsPanel.tsx`）；`packages/storage/src/index-db/` 全目录 grep `habit`（大小写不敏感）**零命中**——habits 无 FTS 表、无影子表、无任何 index-db 接入。「复用同一索引层」的前提确实不存在，补齐 = 全局库整套新建（影子表 + FTS + 向量 + 钩子），交付方「成本不低于本单核心」的评估成立；合同（§16.4 T8.7 行）只写项目记忆。不顺带做的决策与 §4.5 登记**核实无误**。

---

## 2. 抽查发现

**未发现影响结论的缺陷，红线全部未破。** 以下如实登记：

1. **（正面观察）表名参数化的改法把「行为同款」变成了结构保证。** 记忆侧全部向量函数是对 knowledge-vector 的一行转发（memory-vector.ts:63-91），vec0 三坑防线物理上只有一份；「双索引共存」单测（storage :558-588）另钉住两套表组在同一连接上互不干扰（各自规格独立、记忆 drop 不伤知识库表与外键约束）。
2. **（口径澄清，不影响结论）live-embedding 对照组命中数与交付报告不同：本验收实跑 4 命中，交付报告 2 命中。** 原因是仓库文档在交付后继续增长（本验收记录写作时点的 docs/ 含更多描述「把资料喂给模型」语义的段落，Phase6-验收.md 自身就是命中之一）。判据未失效：混合检索仍带进纯 FTS 摸不到的 vector 独有命中（fetch-models 源码等），「向量赢过关键词」成立。块数 2282 → 2289 同性质（Phase6-验收 §2.1 早有同类登记先例：302→297 文件的口径说明）。
3. **（观察项，§5 登记）项目级索引连接的生命周期是进程级。** service 是 data.ts 模块级单例，`close()` 只在单测调用；删除项目（projects:remove）不清对应连接缓存。量级个位到十位连接、进程退出 OS 回收，且 projects:remove 本就不删项目目录，无用户可见故障——不构成缺陷，登记备查（若日后加「删除项目连带删 .workbench」功能，须先关此连接）。
4. **（覆盖缺口登记，探针③/③′发现）「同事务先向量后记账」的崩溃语义无单测钉住。** 两个探针都全绿：③ 同事务内顺序对调（先记账后 put）绿——同一事务内顺序从外部不可观察，属预期；③′ **把事务整个拆开**（两个独立提交、先记账后 put）**也全绿**——这才是真缺口：现有用例没有任何一条能观察到「记了账没向量」的中间态（需要注入「put 抛错后记账是否残留」的用例，如用维度不符的向量触发 assertDimensions 抛错后断言 countMemoryEmbeddingState 为 0）。缺口面评估：拆开事务只在「put 抛错 / 进程崩在两语句之间」时出错，前者一行用例可钉、后者单测本就测不了；核心正确性（差额判定幂等重算）不受影响。**登记 §5 建议补一条守卫用例，不阻塞验收。**
5. **（flake 登记，两条，详见 §4）** 全量首跑 2115/2117：① `command-ipc.test.ts`「CommandPaletteProvider 与 CommandPalette 可被加载」（T8.1 存量用例，本单零触碰）；② **本单新增** `memory-vector.test.ts` reconcile 四态用例——真实文件系统操作（mkdtemp + saveEntry + rm 真删文件再读回），重负载下 Windows 文件时序敏感，与 §4.5 名单「真子进程/真文件 + 时序敏感」同族。两文件单独复跑均绿；该 reconcile 用例单文件 **8 连跑全绿**（23/23 × 8）；后续全量第 4/5/6 跑连续三绿未复现。第 3 跑另触发 §4.5 名单第一条（generic-exec cancel，已知），复跑绿。新形态②建议入 §4.5 名单（§5 待裁定）。

---

## 3. 反向验证（5 条探针 + 1 条缺口探查，全部编辑器工具改 → 跑对应测试文件 → 观察 → `git checkout --` 还原 → 回绿）

基线：四目标文件（`memory-vector.test.ts` storage 23 + desktop `memory-search-service.test.ts` 8 / `memory-view.test.ts` / `contracts.test.ts`）共 **49 条全绿**（探针前实测 `Tests 49 passed (49)`）。storage/desktop 测试均经 vitest 直测 TS 源，探针无需跨包重建；live 实跑前已全量 build。

| # | 探针 | 结果 |
|---|---|---|
| ① | RRF 融合改单路：`memory-search.ts searchMemoryHybrid` 删除向量路 `lists.push`（只回 FTS，usedVector 仍置 true 以规避表层断言） | **4 failed / 19 passed**——恰为「语义召回：零字面重合的查询靠向量路命中」「双路命中融合靠前」「短查询回退 LIKE 融合照常」「limit 生效且融合池取超额召回」四条混合检索用例同红。C 项双路融合被四个方向钉死 |
| ② | 哈希失配不作废旧向量：`memory-index.ts upsertMemoryEntry` 把失配判定改恒 false（编辑后旧向量继续被召回） | **2 failed**——恰为 storage「编辑条目正文 → 旧向量作废、重新进差额」与「reconcile 四态一测（变了作废半边）」。B 项哈希作废机制被钉死（desktop 侧「entrySaved 编辑只重嵌一条」用例经既有对账路径仍绿——编辑作废在 storage 层被钉住已足够） |
| ③ | 同事务内顺序对调：`storeMemoryVector` 先记账后 put | **全绿（预期）**——同一事务内顺序从外部不可观察，非缺口 |
| ③′ | 同事务**拆开**：storeMemoryVector 拆成两个独立提交、先记账后 put | **全绿（缺口坐实）**——现有用例观察不到「记了账没向量」的中间态。按验收指令登记缺口（§2-4 / §5），不阻塞：核心防线是事务原子性 + 差额幂等重算，实现本体是对的，缺的是防回归的守卫用例 |
| ④ | FK CASCADE 删除：`memory-vector-schema.ts` 记账表 REFERENCES 去掉 `ON DELETE CASCADE` | **3 failed / 20 passed**——恰为「删除条目连带删向量与记账行」「rebuildIndex 清空向量（CASCADE 出清记账）」「reconcile 四态一测（消失出清半边）」。B 项 CASCADE 行为被钉死 |
| ⑤ | blocker 码删除：desktop `main/memory-index.ts` search 返回值恒不带 embeddingBlocker | **1 failed / 7 passed**——恰为「未配嵌入来源：纯关键词检索照常可用，blocker 如实为 no-provider」。C 项降级原因码被钉死 |

6 条探针（5 目标 + 1 缺口探查）：①②④⑤**全部恰红于目标用例**；③/③′如实登记为覆盖缺口（见 §2-4 评估）。每条还原后复跑回绿；全部结束后四文件基线 **49/49 回绿**，`git status --short` 仅本验收记录一项。

---

## 4. 自动化回归结果

全部在仓库根执行，HEAD = `b9f3d69`（探针全部还原后；live 实跑前 `pnpm build` 已全量重建）。

| 项 | 结果 |
|---|---|
| 单测全量连跑 | 第 1 跑 **2115/2117**（103 文件）——两条红：command-ipc「CommandPalette 可被加载」（存量）+ **本单新增** memory-vector reconcile 四态用例（真实文件系统 + 重负载时序，§2-5 登记）；两文件单独复跑双绿、reconcile 用例单文件 8 连跑全绿。第 2 跑全绿；第 3 跑触发 §4.5 名单第一条（generic-exec cancel，已知 flake），单独复跑绿；**第 4/5/6 跑连续 2117/2117 × 3 全绿**（Duration 25.59 / 23.52 / 24.11 s） |
| `pnpm lint` | exit 0 —— biome `Checked 612 files. No fixes applied.` + `[check-i18n] PASS`（renderer 无硬编码 CJK 文案） |
| `pnpm build` | exit 0 —— 全工程三套 typecheck，renderer 产物 1,553.56 kB |
| `pnpm smoke` | exit 0 —— 七项全 PASS（main-sqlite · ipc-ping-pong · app-info · sqlite-via-ipc · event-subscription · csp-blocks-eval · secrets-roundtrip），**`No handler registered` 0 行**（Select-String 计数独立核实 = 0） |
| `pnpm test:e2e` | **29 passed（44.1 s）**，exit 0，一次通过（E2E task-parallel 已知 flake 未触发） |
| live-memory-search.mjs | **ALL PASS**，exit 0（真机 Ollama bge-m3 1024 维；对照组 0 命中 blocker=no-provider → 混合语义命中排第一来源 vector；首查 328ms 含 3 条回填 → 第二查 41ms；编辑重嵌/删除出索引/过滤下推全过；§1-E） |
| live-embedding.mjs | **ALL PASS**，exit 0（437 文件 / 2289 块 / 89.4s 全嵌 vec0；语义查询对照组 4 → 混合 5 命中含 vector 独有命中；续传轮 embedded=0 / embedSkipped=2289 / indexed=0；§1-E） |

---

## 5. 遗留 / 建议登记的技术债

| 项 | 说明 | 建议归属 |
|---|---|---|
| 「先向量后记账」崩溃语义守卫用例缺席 | §2-4 / §3 探针③′：拆开事务后全部单测仍绿。建议补一条「put 抛错（维度不符）后断言记账行零残留」的用例，一石钉住「同事务」与「顺序」两半声称 | 建议下一单顺手补（一条用例的量），或 §4.5 登记观察 |
| memory-vector reconcile 用例负载型 flake（新形态） | §2-5：全量首跑失败一次，单文件 8 连跑 + 后续三次全量均绿；真实文件系统 + 重负载时序敏感，与 §4.5 名单同族 | **需主管理员裁定**：入 §4.5 名单观察（推荐，与 process 树杀行同款处置），或直接加固（该用例 rm 后重读，可能是 Windows 文件句柄延迟释放） |
| 项目级索引连接生命周期为进程级 | §2-3：service 单例持有各项目连接到进程退出；projects:remove 不清缓存。现状无用户可见故障 | 备查：若日后加「删除项目连带删 .workbench」功能，须先关此连接 |
| Provider.proxy 未覆盖嵌入管道 | 既有登记（§4.5），T8.7 沿用同一配置面如实重申，非本单新增 | 维持既有口径 |
| 规格失配口径两侧不同（记忆自动 vs 知识库手动） | 主管理员已裁定接受（记忆百级秒级重算、记忆页无重建按钮 vs 知识库万级分钟级、有重建按钮）；论证已落档 `main/memory-index.ts:16-19` 模块注释与进度 §0——核实完毕 | 已裁定，无动作 |
| live-embedding 判据修正定性 + Phase6-验收.md 勘误注记 | 主管理员已裁定接受「脚本自身缺陷随验修正」；本验收独立核实修正合理（§1-E：rebuild force 语义读码证实、修正是加严、产品代码零改动）。**勘误注记归关口收尾单** | 关口收尾单 |
| process 树杀前置探活 flake | 主管理员已裁定名单观察即可，暂不加固；本验收全量六跑均未触发 | §4.5 已登记，无动作 |

---

## 6. 验收结论

### **通过（带遗留）。**

- **A 项成立（本单最要紧）**：「复用不另写一套」合同全部核实——rag 包 diff **零输出**（src 与 tests 一行未动）；RRF 经 `@ff-pane/rag/retrieve` 既有出口导入，全仓 grep 恰一份 `fuseByRrf`/`rrfScore` 实现；`knowledge-vector.ts` 表名参数化逐 diff 核实为「加缺省参数 + 表名常量替换」，算法零改动、vec0 三坑防线只存一份，既有调用零改动（knowledge 单测全绿佐证）；记忆侧全部向量函数是一行表组转发。
- **B 项成立**：v3 迁移只建两张状态表、向量表惰性（款式与 T6.4 对照一致）；v2 旧库升级无损单测断言强度足（版本 + 数据可查性 + 新表空三重）；FK CASCADE 读码 + 单测 + 探针④恰红三重钉死；同事务「先向量后记账」实现正确、崩溃语义论证成立，但**守卫用例缺席**（探针③′坐实，§5 登记）。
- **C 项成立**：双路召回 + RRF 融合（探针①恰红 4 条）；超额召回 limit×4 有「深位救回」单测；过滤精确前置下推（量级论证成立）；未配嵌入来源纯 FTS 一等状态 + blocker 码（探针⑤恰红）；upsert 哈希失配作废旧向量（探针②恰红 2 条）；两层「不命中空气」兜底。
- **D 项成立**：差额回填单飞（Promise 复用 + finally 置空，服务端请求计数单测钉死）；嵌入失败只降级不抛（try 包全 + report 只 log）；惰性打开进程内缓存打开失败自摘、close 全清——生命周期为进程级的泄漏面评估后不构成缺陷（§2-3 登记）。
- **E 项成立（两脚本本验收真机实跑 ALL PASS）**：live-memory-search 语义召回实例成立（「执行 vitest 命令」被「怎么跑单测」以 vector 来源命中第一，对照组 0 命中且对照组取在配 Provider 之前——判据设计诚实）；live-embedding 全项过（2289 块 vec0、语义判据成立、续传 embedded=0）；**续传判据修正独立评估为合理**（rebuild 恒 force 读码证实、修正加严两条断言、产品代码零改动）。
- **F 项成立**：契约与 handler 逐字段一致 + 通道三处登记；250ms 防抖 + requestSeq 防乱序；三态回退纯函数 +4 单测；零新 i18n 键属实（两个既有键值 zh/en 同步改）。
- **G 项成立**：23 文件与交付报告一致；依赖/lock/fixtures/rag/adapters 全零改动；knowledge-index 断言放宽合理（「恰为 3」由新用例接管，防护无空窗）；零 TODO、未 push。
- **H（habits 前提）成立**：index-db 全目录 grep habit 零命中，「无索引层可复用」核实无误，不顺带的决策与登记成立。
- **反向验证**：6 条探针（含 1 条缺口探查）——①②④⑤全部恰红于目标用例，③/③′如实坐实一处覆盖缺口（§5 建议补一条守卫用例，不阻塞）；还原后基线 49/49 回绿。
- **回归数字**：2117/2117 × 3 连续全绿（103 文件；首跑 2115 触发两条负载型 flake——含本单新增 reconcile 用例一条新形态，§2-5 登记；第 3 跑触发已知 generic-exec cancel，复跑绿）· lint exit 0（612 文件 + check-i18n PASS）· build exit 0 · smoke 七项 PASS 且 `No handler registered` 零行 · E2E 29/29 一次通过 · 两 live 脚本 ALL PASS。

**需主管理员裁定事项**：① 本单新增 memory-vector reconcile 用例的负载型 flake 处置（§5 第二行）——推荐入 §4.5 名单观察，或评估加固（Windows 文件句柄延迟释放方向）；② 「先向量后记账」崩溃语义守卫用例（§5 第一行）——建议下一单顺手补一条（探针③′的形态即用例模板）。三条交付方裁定请求的主管理员结论已如实登记（§5：判据修正接受 + Phase6 勘误归关口收尾单 · process 树杀名单观察 · 规格失配两侧不同接受），本验收对其核实部分均无异议。

验收结束时 `git status --short`：

```
?? docs/验收记录/T8.7-验收.md
```

