# FF-pane 开发计划

**文档版本：** v1.1（2026-08-29）
**v1.1 修订：** 新增 §1.4 质量标准（非 MVP 红线）与 §1.5 界面交互质量要求；§1.2 明确主管理员 / 子 Agent 分工模式；T3.1 扩充为"设计系统 + UI 骨架"。
**依据文档：** 项目设计计划-v1.0.md（产品设计）、技术选型.md（技术栈）
**文档性质：** 正式执行依据。开发严格按本计划推进，不边做边想。
**变更规则：** 执行中发现计划与现实冲突（如某 CLI 能力与预期不符），立即停止相关任务、向用户报告，经确认后修订计划并升版本号，不静默偏离。

---

# 1. 计划总则

## 1.1 执行纪律

```text
1. 每个任务是一份合同：目标、产出、范围、验收标准、验证方式在本计划中写死。
2. 任务完成的定义（DoD）：
   代码完成 + 单测通过 + lint 通过 + 验收标准逐条核对 + 一次符合规范的提交。
3. 提交节奏：每任务至少一次提交，提交信息引用任务 ID
   （示例：feat(core): T1.4 Plan/Task/Run 状态机）。
4. 每个 Phase 结束：跑全量测试 → 向用户汇报 → 打版本 tag → 进入下一 Phase。
5. 任何任务不得修改自己范围之外的文件；发现必须跨范围时，先报告。
```

## 1.2 主管理员 / 子 Agent 分工

```text
- 主管理员（本 Agent）：拆解与下发任务合同、审查产出、集成、提交、
  打 tag、向用户汇报。集成类任务亲自执行，不派发。
- 子 Agent：执行具体编码，每个子 Agent 只负责一个任务合同的范围。
- 子 Agent 模型：inherit（继承主管理员配置，即 Claude Fable 5）。
  2026-08-29 经用户确认（用户原意 Fable 5 medium 档位不可用）。
- 标注"并行"的任务组派发给多个子 Agent 同时执行，
  前提：文件范围互不重叠（已在任务范围中保证）。
- 派发时向子 Agent 提供完整任务合同（子 Agent 看不到对话历史）。
- 所有子 Agent 产出由主管理员实际审查：读代码、跑测试、跑 lint，
  通过后才整合提交。"子 Agent 说完成"不等于完成。
```

## 1.3 任务规模标记

```text
S = 小（单文件/单模块，直接做）
M = 中（一个完整功能模块）
L = 大（跨模块或含真机联调）
```

## 1.4 质量标准：非 MVP 红线

本项目不做"最小可用版本"。每个任务交付的是完成品，不是骨架：

```text
1. 禁止占位实现：不允许 TODO 空函数、假数据、stub 页面进入主干；
   任务要么按验收标准完整交付，要么不合并。
2. 严格 TypeScript：strict 模式，禁止随意 any；类型即文档。
3. 边界处理完整：系统边界（用户输入、CLI 输出、API 响应、文件系统）
   的失败路径必须处理，并给出用户可理解的界面反馈。
4. 测试是交付的一部分：core / storage / rag / adapters 的逻辑必须带单测；
   "能跑"不等于"完成"。
5. 性能有底线：万级记忆条目、十万级知识库块、百条消息的会话
   不允许可感知卡顿（虚拟列表、索引查询、流式渲染是标配）。
```

"分阶段交付"仍然成立（如打包先出 Windows 版），但每个阶段交付出去的部分必须是完成品质量。

## 1.5 界面交互质量要求

在"信息密度优先、无装饰"的前提下，交互质量按以下标准执行：

```text
1. 设计系统先行：T3.1 扩充为"设计系统 + UI 骨架"——色板（暗/亮双主题）、
   间距与字号阶梯、组件规范、交互规范先于页面开发定稿。
2. 三态齐全：每个数据视图必须处理 加载中（骨架屏）/ 空 / 错误 三态；
   空态用一句说明 + 下一步动作按钮，不用插画。
3. 即时反馈：所有操作 100ms 内有响应表现；长操作显示进度且可取消；
   Agent 输出流式渲染。
4. 键盘可达：核心流程（切项目、切会话、派发任务、审核记忆）有快捷键；
   提供全局命令面板（Ctrl+K）。
5. 危险操作二次确认；普通操作优先"可撤销"而不是弹窗拦截。
6. 深浅主题跟随系统并可手动切换（实用功能，不属于装饰）。
```

---

# 2. 总路线：Phase 与版本映射

| Phase | 内容 | 完成后版本 | 对应设计里程碑 |
|---|---|---|---|
| 0 | 工程基建（monorepo、Electron 壳、i18n、CI、打包） | **v0.1.0** | — |
| 1 | 数据层与领域核心（类型、存储、索引、状态机、Provider） | v0.2.0 | M1 |
| 2 | Agent 接入层（适配器框架 + 4 个 L1 + L2 + 权限） | v0.3.0 | M1 |
| 3 | M1 界面（7 页面） | v0.4.0 | M1 |
| 4 | M1 工作流串联与验收 | **v0.5.0 = M1 完成** | M1 |
| 5 | 共享记忆（用户习惯档案 + 习惯先行） | v0.6.0 | M2 |
| 6 | 知识库 RAG | v0.7.0 | M2 |
| 7 | Handoff + Reviewer + Grok/Aider 适配器 | **v0.8.0 = M2 完成** | M2 |
| 8 | M3 按需增强（届时另行细化计划） | 0.9.x | M3 |

Phase 之间严格串行；Phase 内部按任务依赖图并行。

---

# 3. Phase 0：工程基建 → v0.1.0

目标：一个能启动的空壳应用 + 完整工具链。此阶段全部串行（每步是下一步的地基）。

### T0.1 Monorepo 骨架 [S]
- **产出：** 仓库根 pnpm-workspace、`apps/desktop` 与 `packages/{shared,core,adapters,rag,storage}` 空包、tsconfig 基座（strict）、Biome 配置、Vitest 配置
- **验收：** `pnpm install`、`pnpm lint`、`pnpm test`（含各包一个占位测试）全部通过
- **依赖：** 无

### T0.2 Electron 应用壳 [M]
- **产出：** electron-vite 装配的 main/preload/renderer 三层；contextIsolation 开启、nodeIntegration 关闭；类型安全的 IPC 封装（渲染层只能调 preload 暴露的通道）；单窗口启动
- **验收：** `pnpm dev` 打开窗口；IPC ping-pong 往返测试通过；**better-sqlite3 在 Electron 内加载成功**（原生模块重编译风险在此提前验证，见 §12 风险 R1）
- **依赖：** T0.1

### T0.3 i18n 框架 [S]
- **产出：** i18next + react-i18next 装配；`locales/zh-CN.json`、`locales/en-US.json`；系统语言检测、缺失 key 回退 en-US；检测 UI 硬编码字符串的扫描脚本（接入 lint 流程）
- **验收：** 演示页面切换语言立即生效；扫描脚本能抓出故意埋的硬编码文案
- **依赖：** T0.2

### T0.4 CI [S]
- **产出：** GitHub Actions：windows + ubuntu 双平台，lint + test + build 三步
- **验收：** push 后 CI 全绿
- **依赖：** T0.1

### T0.5 打包链路 [S]
- **产出：** electron-builder 最小配置（先只出 Windows NSIS）
- **验收：** 本地产出安装包，安装后应用可启动
- **依赖：** T0.2

**Phase 0 关口：** 全部验收通过 → 提交 → 打 tag `v0.1.0` → 向用户汇报。

---

# 4. Phase 1：数据层与领域核心 → v0.2.0

目标：设计文档第 3、6、7、10 章的领域模型全部落地为经过单测的纯逻辑代码。UI 无关。

### T1.1 领域类型定义 [S]（串行，先行）
- **产出：** `packages/shared`：Provider、AgentProfile、Plan、Task、Run、MemoryEntry、KnowledgeEntry、Handoff、AgentEvent、权限信封等全部类型，与设计文档字段一一对应
- **验收：** 编译通过；附一份"类型 ↔ 设计文档章节"对照清单（注释形式）
- **依赖：** 无

### T1.2 文件存储层 [M]（可与 T1.3 并行）
- **产出：** `packages/storage`：`~/.aiworkbench/` 与 `<项目>/.workbench/` 目录布局（设计文档 §10）的初始化与读写 API；计划/任务/Run/记忆的 Markdown+JSON 读写；原子写入（临时文件 + rename）
- **验收：** 读写往返单测；目录不存在时自动初始化；损坏 JSON 不崩溃（报错并隔离）
- **依赖：** T1.1

### T1.3 检索索引层 [M]（可与 T1.2 并行）
- **产出：** `packages/storage` 内的 SQLite 索引模块：better-sqlite3 + FTS5（trigram tokenizer）；记忆条目的增删改同步索引；`rebuildIndex()` 全量重建
- **验收：** 中文与英文关键词检索单测通过；删除 index.sqlite 后重建结果与原索引一致
- **依赖：** T1.1

### T1.4 领域状态机 [M]（串行，我执行）
- **产出：** `packages/core`：Plan 版本与状态流转（draft/approved/superseded/completed/cancelled，批准仅限用户动作）；Task 六状态机（§6.3，非法迁移抛错）；Run 生命周期；权限信封交集计算（§7）
- **验收：** 状态迁移矩阵单测（合法迁移全覆盖 + 非法迁移全拒绝）；权限交集含危险操作清单测试
- **依赖：** T1.1

### T1.5 Provider 管理 [M]
- **产出：** Provider CRUD；safeStorage 密钥加密存储（明文只在主进程、界面只显尾 4 位）；openai_compatible 与 anthropic 的 `/models` 拉取 + 手动录入回退；连接测试（最小请求，错误原文透传）；cli_login 类型的登录态探测（执行 CLI 的 auth 检查命令）
- **验收：** mock 服务单测 + 至少一个真实 endpoint 手工冒烟
- **依赖：** T1.2

### T1.6 Profile 管理 [S]
- **产出：** Profile CRUD；角色默认权限预设；Profile = Runtime+Provider+模型+角色+权限+输出语言 的组装与校验
- **验收：** 单测（非法组合拒绝，如 Planner 预设带写权限时要求确认）
- **依赖：** T1.5

**并行批次：** B1-1 = T1.1（先行）→ B1-2 = {T1.2, T1.3, T1.4 我做} 并行 → B1-3 = T1.5 → T1.6。
**Phase 1 关口：** 全量测试绿 → tag `v0.2.0` → 汇报。

---

# 5. Phase 2：Agent 接入层 → v0.3.0

目标：设计文档第 5 章落地。四个 L1 适配器 + L2 兜底 + 权限执行。

### T2.0 【调研】CLI headless 摸底 [M]（4 个子 Agent 并行，先行）
- **产出：** 对 Codex CLI / Claude Code / Gemini CLI / OpenCode 各产出一份 `docs/adapters/<name>.md`：当前版本的 headless 启动参数、事件流格式、原生会话恢复方式、取消方式；每个 CLI 录制一组真实事件样例存入 `packages/adapters/fixtures/`
- **验收：** 四份文档 + 四组 fixture 入库；与设计文档 5.1 六项能力声明逐项核对
- **依赖：** 无（可与 Phase 1 尾部并行启动）
- **说明：** 这是唯一允许"探明现实"的任务，探明结果若与本计划的适配器任务冲突，触发计划修订流程

### T2.1 适配器框架 [M]（串行，我执行）
- **产出：** `packages/adapters`：AgentAdapter 统一接口（capabilities/start/send/cancel/resume）；子进程管理（spawn、Windows 进程树终止、超时）；JSONL 行解析器；统一 AgentEvent 事件流；能力声明结构
- **验收：** 用仓库内的 fake-cli 测试脚本完成全接口联调单测
- **依赖：** T1.1、T2.0

### T2.2 generic-exec 适配器（L2）[S]
- **产出：** 命令模板（`{task}` 占位符）+ 工作目录 + 环境变量配置；一进一出收集 stdout
- **验收：** 配置一个真实 CLI 工具跑通单次任务
- **依赖：** T2.1

### T2.3 Codex 适配器 / T2.4 Claude Code 适配器 / T2.5 Gemini CLI 适配器 / T2.6 OpenCode 适配器 [各 M]（4 个子 Agent 并行）
- **产出（各自）：** headless 启动、事件流解析映射为 AgentEvent、原生会话 ID 登记与恢复（若支持）、取消、能力声明；差异全部隔离在适配器内
- **验收（各自）：** fixture 回放单测全绿 + 真机冒烟（发一个小任务收到完整事件流）
- **依赖：** T2.1，各自的 T2.0 fixture

### T2.7 权限执行层 [M]（串行，我执行）
- **产出：** Run 启动前计算权限信封（core 已有交集逻辑）；写路径越界拦截；危险操作固定清单拦截（§7）并上抛 permission_request；密钥经环境变量注入子进程（仅限该 Run）
- **验收：** 越权写文件被拦截的集成测试；危险命令触发审批事件的测试
- **依赖：** T2.1、T1.4

**并行批次：** B2-1 = T2.0（4 子 Agent）→ B2-2 = T2.1 → B2-3 = {T2.2, T2.3, T2.4, T2.5, T2.6} 并行 → B2-4 = T2.7。
**Phase 2 关口：** 全量测试绿 + 四个真机冒烟记录 → tag `v0.3.0` → 汇报。

---

# 6. Phase 3：M1 界面 → v0.4.0

目标：设计文档第 11 章的 7 个页面中，M1 需要的部分全部可用。信息密度优先，无装饰。

### T3.1 设计系统 + UI 骨架 [L]（串行，先行）
- **产出：** 设计系统定稿（色板含暗/亮双主题、间距字号阶梯、组件规范、交互规范，落为 `docs/设计系统.md` + Tailwind 主题配置）；shadcn/ui 装配与主题定制；应用布局（侧边导航 + 内容区）；7 页面路由；三态（加载骨架/空/错误）标准组件；全局命令面板（Ctrl+K）框架；Zustand store 结构；与主进程的 IPC 数据层封装（查询/订阅模式）
- **验收：** 全部页面可导航；双主题切换正常；三态标准组件演示齐全；一条真实 IPC 数据流（如 Provider 列表）端到端渲染
- **依赖：** Phase 1、2 完成

### T3.2 设置页 [M] — Provider 管理（含测试连接、密钥尾 4 位显示）、Profile 管理、界面语言、AI 输出语言、默认权限预设
- **验收：** 新建 Provider → 测试连接 → 建 Profile 全流程可操作
### T3.3 项目列表页 [S] — 项目卡片、新建（选目录即建 `.workbench/`）、移除
- **验收：** 新建项目后目录结构正确生成
### T3.4 会话页 [L] — 顶部状态条（角色/Profile/模型/权限/会话类型）、流式对话渲染、角色切换、消息右键（存入知识库占位、提为记忆候选）
- **验收：** 与真实 Planner Profile 对话，流式输出、状态条信息正确
### T3.5 计划页 [M] — 版本列表、计划正文渲染、批准按钮（仅用户）、让 Planner 修改（生成新草案）、版本文本 diff
- **验收：** 计划从 draft 到 approved 的完整操作流
### T3.6 任务页 [M] — 六状态分列、任务卡片、blocked 置顶（澄清/权限请求一键处理）、派发/重试/接受/取消
- **验收：** 任务全状态流转可操作
### T3.7 执行记录页 [M] — Run 列表、diff 视图、命令与退出码、验证结果、打开原始日志（外部）
- **验收：** 真实 Run 的修改与命令完整可见
### T3.8 记忆页 [M] — 项目记忆/共享记忆（占位）/候选三标签、类别分组、全文搜索、审核操作（通过/编辑后通过/拒绝）
- **验收：** 候选审核闭环可操作

**并行批次：** B3-1 = T3.1 → B3-2 = {T3.2, T3.3} 并行 → B3-3 = {T3.4 我做, T3.5, T3.6} 并行 → B3-4 = {T3.7, T3.8} 并行。
（每页面文件范围独立：`apps/desktop/src/renderer/pages/<page>/`，共享组件变更收敛到 T3.1 建立的 `components/` 由我统一审查。）
**Phase 3 关口：** 各页面验收演示 → tag `v0.4.0` → 汇报。

---

# 7. Phase 4：M1 工作流串联与验收 → v0.5.0（M1 完成）

目标：设计文档 §12 十步流程端到端真实跑通。本阶段以集成为主，全部由我串行执行。

### T4.1 Prompt 组装层 v1 [M]
- **产出：** 四层组装（§8.2.2）：角色定义 → 习惯档案（**本阶段留空位**）→ 项目记忆注入（Planner/Worker/Reviewer 差异化策略，§8.1）→ 当前输入；AI 输出语言指令按 全局→Profile→项目 三级取值
- **验收：** 组装结果快照测试；注入条数上限截断测试

### T4.2 十步流程集成 [L]
- **产出：** 新建项目 → Planner 讨论 → 出计划 → 批准 → 任务合同生成 → 派发 Worker → 权限请求处理 → Run 证据落盘 → 用户接受 → 记忆候选生成，全链路打通
- **验收：** 用真实 Agent 完成一个小型真实需求（如"给示例项目加一个工具函数并通过测试"）全流程

### T4.3 会话恢复 [M]
- **产出：** 原生恢复（登记的 Native Session ID）；上下文重建（计划+任务+state.md+最近 Run 报告）；UI 明确标注恢复类型
- **验收：** 重启应用后两种恢复各验证一次

### T4.4 记忆闭环 [S]
- **产出：** 任务 accepted 后记忆候选汇总提示（角标，不弹窗）；审核后进入注入池生效
- **验收：** 新候选在下一次会话的 Prompt 组装中出现

### T4.5 M1 验收 [M]
- **产出：** Playwright 基础 E2E（启动、建项目、建 Provider 三条冒烟）；执行 §13 的 M1 手工验收清单并记录结果到 `docs/验收记录/`
- **验收：** 清单全项通过

**Phase 4 关口：** tag `v0.5.0`，M1 完成，向用户做整体演示汇报。

---

# 8. Phase 5：共享记忆（用户习惯）→ v0.6.0

依据设计文档 §8.2。

### T5.1 习惯存储与管理 [M] — `~/.aiworkbench/habits/` 四分类存储；CRUD；入库相似/矛盾检测（FTS 相似检索 + 并排展示，用户选合并/替代/保留）；80 条上限提示
- **验收：** 冲突检测单测 + 管理界面（设置页内）可操作
### T5.2 习惯档案编译器 [M] — active 条目 → 按类别分组、重要度排序 → 紧凑指令文本；workflow 类标注"必须遵守的流程约束"；变更自动重编译；接入 Prompt 第 2 层（T4.1 预留位）
- **验收：** 编译快照测试；修改习惯后下一会话 Prompt 即变
### T5.3 习惯先行 [M] — Planner 收到指令先核对 workflow 习惯输出整形方案（结构→确认→执行）；"直接做"单次跳过；跳过不影响档案
- **验收：** 真实会话演示：同一指令在有/无 workflow 习惯下行为不同
### T5.4 三来源闭环 [M] — 手写（已含 T5.1）；从项目记忆提炼（剥离项目专有信息对话框 + 用户确认 + source_project/source_entry_id 溯源）；观察建议（会话内重复纠正检测 → 生成候选提示）
- **验收：** 三条路径各走通一次；任何来源未经确认不 active

**并行批次：** T5.1 → {T5.2, T5.4 部分} → T5.3。
**Phase 5 关口：** tag `v0.6.0` → 汇报。

---

# 9. Phase 6：知识库 RAG → v0.7.0

依据设计文档 §8.3。

### T6.1 解析器注册表 [M]（并行组）— md/txt/代码直读；pdfjs-dist（文本+页码）；mammoth（docx）；html 正文抽取；按扩展名分发；单文件失败不中断批量
- **验收：** 各格式样例文件解析单测（fixture 入库）
### T6.2 结构感知分块器 [M]（并行组）— Markdown 标题树 / PDF 页段落 / 代码函数边界（启发式）；300~800 token、15% 重叠；每块带出处（文件/标题路径/页码）
- **验收：** 各格式分块快照测试；出处字段完整性测试
### T6.3 嵌入管道 [M]（并行组）— Provider `/embeddings` + Ollama；批量、并发限制、按块哈希断点续传；未配嵌入模型 → 纯 FTS 降级（功能不缺失）
- **验收：** mock 端点单测 + 断点续传测试 + 降级路径测试
### T6.4 向量索引与混合检索 [M]（串行，我执行）— sqlite-vec 装载（Windows 兼容验证，见 R2）；FTS5 BM25 与向量双路召回 → RRF 融合；上下文扩展（相邻块）；来源/格式/标签/时间过滤
- **验收：** 中文检索质量抽测（准备 20 条查询样例，人工核对 top-5 合理性）；纯 FTS 模式同套测试
### T6.5 知识库页 [L] — 导入文件/文件夹（进度、增量索引）、来源管理（文档数/块数/状态/重建）、混合检索结果（块+出处）、发送到当前会话（带出处引用）、导出 Markdown、会话页"从知识库插入"打通
- **验收：** 导入一个真实文档目录（百级文件）全流程可用
### T6.6 Agent 只读检索工具 [M] — 项目级开关（默认关）；query 进/命中块出；每次调用与命中记录进执行记录页；写操作物理不存在
- **验收：** Worker 真实调用检索并在执行记录可见

**并行批次：** B6-1 = {T6.1, T6.2, T6.3} 并行 → B6-2 = T6.4 → B6-3 = T6.5 → B6-4 = T6.6。
**Phase 6 关口：** tag `v0.7.0` → 汇报。

---

# 10. Phase 7：Handoff、Reviewer、更多 Runtime → v0.8.0（M2 完成）

### T7.1 Handoff [M] — 8 字段交接包生成（§10.4）；预览可编辑；确认后注入新 Agent 会话；UI 标注"跨 Agent 迁移"；密钥/日志/他项目内容物理隔离
- **验收：** Codex Planner → 其他 Planner 真实迁移一次，新 Agent 能正确接续工作
### T7.2 Reviewer 角色 [M] — 可选开启；审查任务合同（对照验收标准+diff）；只读+验证命令权限；审查结论进入任务接受流程
- **验收：** 一个任务走完 Worker→Reviewer→用户 三级确认
### T7.3 Grok Build + Aider 适配器 [M×2]（2 子 Agent 并行）— 流程同 T2.0（摸底+fixture）+ T2.3（实现）
- **验收：** 同 Phase 2 适配器标准
### T7.4 多项目完善 [S] — 项目列表信息完整化（计划版本/活跃任务/最后活动）；项目间数据隔离核查
- **验收：** 两个项目并存互不干扰的核查清单通过

**Phase 7 关口：** tag `v0.8.0`，M2 完成，整体演示汇报。

---

# 11. Phase 8：M3（占位，届时另行细化）

ACP 标准接入、任务并行（文件范围不重叠）、自定义角色、更多 Runtime（Qwen Code、iFlow 等）、记忆语义检索（复用 T6.3 管道）。**进入 Phase 8 前先出细化计划经用户批准。**

---

# 12. 风险清单与预案

| # | 风险 | 预案 | 验证点 |
|---|---|---|---|
| R1 | better-sqlite3 原生模块在 Electron 下需重编译，Windows 上易踩坑 | electron-rebuild 纳入 T0.2 验收，第一周就暴露 | T0.2 |
| R2 | sqlite-vec 扩展的 Windows 加载兼容性 | T6.4 开头先做最小加载验证；不可用则退路：向量存普通表 + JS 余弦计算（十万块量级可接受） | T6.4 |
| R3 | 各 CLI headless 参数与事件格式随版本漂移 | T2.0 摸底 + fixture 锁定；差异隔离在适配器内；漂移时只改适配器 | T2.0 |
| R4 | FTS5 对中文分词效果 | trigram tokenizer 在 T1.3 用中文样例验证；不达标则引入分词预处理 | T1.3 |
| R5 | Windows 中文路径 / 编码问题 | 全链路 UTF-8；T1.2 单测覆盖中文路径与中文文件名 | T1.2 |
| R6 | pnpm monorepo + Electron 原生依赖 hoisting 问题 | T0.1 即配置 pnpm 的部署策略并在 T0.2/T0.5 验证 | T0.5 |
| R7 | 真机冒烟消耗 API 成本 | 冒烟统一用低价模型；fixture 回放承担主要测试量 | 全程 |

---

# 13. M1 手工验收清单（T4.5 执行）

```text
□ 1  新建项目：目录选择 → .workbench/ 结构正确生成
□ 2  新建 Provider（openai_compatible）→ 测试连接成功，密钥只显尾 4 位
□ 3  新建 Planner / Worker 两个 Profile 并绑定到项目
□ 4  与 Planner 讨论一个真实小需求，流式输出正常，状态条信息正确
□ 5  生成计划草案 → 计划页可见 → 修改 → 批准 → 状态 approved
□ 6  任务合同生成，字段完整（目标/write_scope/验收标准/验证命令）
□ 7  派发任务 → Worker 真实执行 → 文件修改与命令在执行记录页可见
□ 8  Worker 越权写范围外文件 → 被拦截并转为权限请求 → 用户批准后继续
□ 9  验证命令通过 → 任务 done → 用户点接受 → accepted
□ 10 记忆候选生成 → 记忆页审核通过 → 下一会话 Prompt 中生效
□ 11 重启应用 → 原生恢复与上下文重建各验证一次，UI 标注正确
□ 12 界面语言 zh/en 切换即时生效；AI 输出语言设置生效
□ 13 删除 index.sqlite → 应用自动重建，检索恢复正常
□ 14 安装包安装到干净环境可正常启动
```

---

# 14. 执行批次总览

| 顺序 | 批次 | 任务 | 方式 |
|---|---|---|---|
| 1 | Phase 0 | T0.1 → T0.2 → {T0.3, T0.4, T0.5} | 串行为主，尾部并行 |
| 2 | B1-1/2/3 | T1.1 → {T1.2, T1.3, T1.4} → T1.5 → T1.6 | 中段 3 并行 |
| 3 | B2-1 | T2.0（可提前至 Phase 1 中段启动） | 4 子 Agent 并行 |
| 4 | B2-2/3/4 | T2.1 → {T2.2~T2.6} → T2.7 | 中段 5 并行 |
| 5 | B3-1~4 | T3.1 → {T3.2,T3.3} → {T3.4,T3.5,T3.6} → {T3.7,T3.8} | 分批并行 |
| 6 | Phase 4 | T4.1 → T4.2 → T4.3 → T4.4 → T4.5 | 全串行（集成） |
| 7 | Phase 5 | T5.1 → {T5.2, T5.4} → T5.3 | 中段并行 |
| 8 | B6-1~4 | {T6.1,T6.2,T6.3} → T6.4 → T6.5 → T6.6 | 头部 3 并行 |
| 9 | Phase 7 | {T7.1, T7.2, T7.3×2} → T7.4 | 头部并行 |

---

# 15. 启动条件

本计划经用户批准后，从 T0.1 开始执行。批准即视为对以下事项的确认：

```text
1. Phase 划分与版本映射（§2）
2. 各任务的范围与验收标准（§3~§10）
3. 主管理员 / 子 Agent 分工与并行规则（§1.2）
4. 质量标准与界面交互要求（§1.4、§1.5）
5. 计划变更须经用户确认的纪律（文档头部）
```
