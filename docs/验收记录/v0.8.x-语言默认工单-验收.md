# v0.8.x 语言默认工单验收记录（界面语言默认回退改 zh-CN + 两条验收遗留 + endonym 裁定落实）

**执行时间：** 2026-09-01
**执行人：** 验收员（本 Agent）
**被验提交：** `67471f0`（`feat: v0.8.x 语言默认工单 —— 界面语言默认回退改 zh-CN（用户需求）+ 两条验收遗留`）
+ `a1549ad`（`feat: v0.8.x 语言选择器标签改用语言自称（endonym 裁定落实）`，最新提交），验收开始时工作区干净
**依据：** 用户需求原文（2026-09-01）＋主管理员口径 ＋ `docs/开发进度.md` §0 本单条目（交付声明）
**版本关口：** M2 已于 `v0.8.0` 收口（`docs/验收记录/Phase7-验收.md`）。本单为 tag 之后的 v0.8.x 需求单，不构成新关口

> **用户需求原文**：语言系统包含中文英文、后续可拓展其他语言、**以中文为默认语言**、用户可在设置界面选择主语言。
> **主管理员口径（本次验收按此为准）**：保留系统语言检测，只把最终回退 en-US → zh-CN；endonym 裁定已采纳落实。

---

## 0. 执行环境

| 项 | 值 |
|---|---|
| 平台 | Windows 11（10.0.22631），PowerShell 5.x（不支持 `&&`，多命令以 `;` 分隔） |
| 包管理器 | pnpm 11.24.0 |
| TypeScript | 7.0.2（原生编译器，故三套 typecheck 秒级完成） |
| 被验 HEAD | `a1549ad`（验收全程未改一行产品代码，仅新增本记录与进度文档落档） |
| 自动化 | vitest 1735 单测 / 85 文件 · Playwright 14 条 E2E（`_electron` 驱动构建产物） |
| 真机数据根 | **未触碰**。E2E / 冒烟一律走临时 `FF_PANE_DATA_ROOT` + 临时 `--user-data-dir` |

> 本次验收不启动任何会弹窗 / 抢焦点 / 开浏览器 / 联网授权的进程（E2E 与冒烟的 Electron 隐藏窗口除外），
> **未发出任何外网请求**：本单改动不含网络路径，未起任何 Agent 子进程，未读写用户全局配置。
> 一处离体验证（§1.3 的 `satisfies` 守卫实证）在**系统临时目录**内进行，仓库文件零改动。

---

## 1. 需求逐条对照（用户需求四条 + 主管理员口径）

### 1.1 语言系统包含中文英文（既有功能，抽查仍完好）

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 两本语言包齐全且 key 集合一致 | `locales/zh-CN.json` / `locales/en-US.json` 两本俱在；`locales-parity.test.ts` 断言各包 key 集合完全一致且叶子值非空（本次全绿）；`check-i18n` PASS（renderer 无硬编码 CJK） | ✅ |
| 注册表与语言包一一对应 | `resources` 以 `as const satisfies Record<SupportedLanguage, { translation: unknown }>` 登记两本；新增单测「每种受支持语言都有语言包」另在运行期钉一遍（`Object.keys(resources).length === SUPPORTED_LANGUAGES.length`） | ✅ |
| 本单未动语言包 | `git diff 67471f0..a1549ad -- locales/` 与 `git diff 8ad784e..67471f0 -- locales/` **两次输出均为空** —— 两个提交对 `locales/*.json` 零改动，与交付声明一致 | ✅ |

### 1.2 用户可在设置界面选择主语言（既有功能，抽查仍完好）

`LanguageSection.tsx` 的界面语言下拉：`value={currentUi}`、`onChange` 调 `changeUiLanguage`（立即经
react-i18next 重渲染 + 写 `localStorage["ffpane.ui-language"]`），选项由 `LANGUAGE_OPTIONS` 渲染。
E2E `_launch.ts` 每条用例都实测这条链路的持久化端（写键 + reload + 断言 `<html lang>` 变更），本次 14/14 全绿。✅

### 1.3 默认中文 —— 本单主体

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 领域层最终回退 = zh-CN | `packages/shared/src/domain/language.ts:34` `export const FALLBACK_UI_LANGUAGE: UiLanguage = "zh-CN";`，注释写明「2026-09-01 经用户确认由 en-US 改为 zh-CN」「系统语言检测本身保留——本常量只是级联的最后一环」 | ✅ |
| 渲染层解析回退即领域层常量 | `i18n/resolve.ts:20` `export const FALLBACK_LANGUAGE: SupportedLanguage = FALLBACK_UI_LANGUAGE;`（不是再写一遍 `"zh-CN"`）；`resolveUiLanguage` 仍为 `matchSupported(saved) ?? matchSupported(systemLocale) ?? FALLBACK_LANGUAGE` —— 级联三环结构逐字未动，只换了最后一环的值 | ✅ |
| 「ja-JP 等不支持语言 → 落 zh-CN」用例存在 | `i18n-resolve.test.ts:43-50` 新增用例：`FALLBACK_LANGUAGE === "zh-CN"` · `resolveUiLanguage(null,"ja-JP") === "zh-CN"` · `resolveUiLanguage(null,"fr-FR") === "zh-CN"`。另有既有用例以 `FALLBACK_LANGUAGE` 形式覆盖 `de-DE/ko-KR`、空串、纯空白 | ✅ |
| 领域层侧也钉住了值 | `packages/shared/tests/domain.test.ts` §9.1 用例标题由「回退 en-US」改为「回退 zh-CN」，并**新增一条直断言** `expect(FALLBACK_UI_LANGUAGE).toBe("zh-CN")`（原用例只有 `UI_LANGUAGES).toContain(FALLBACK_UI_LANGUAGE)`，那条对 en-US 同样成立、钉不住值）；`UI_LANGUAGE_SETTINGS` 仍为 `["system","zh-CN","en-US"]` | ✅ |

### 1.4 可拓展：「三处即全」现在成立

**这是本单最实在的一处收敛。** 改动前存在**两份同形的界面语言清单**：渲染层 `i18n/resolve.ts` 的
`SUPPORTED_LANGUAGES`（运行时唯一消费方）与领域层 `UI_LANGUAGES`（生产代码零消费方，只被 `domain.test.ts` 断言），
且各自带一个回退常量。收敛后：

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 唯一事实源在领域层 | `resolve.ts` 改为 `export const SUPPORTED_LANGUAGES = UI_LANGUAGES;` + `export type SupportedLanguage = UiLanguage;` —— 是**别名**而非副本。`language.ts` 注释里写明「这是界面语言的唯一注册表」 | ✅ |
| 「无第二份清单」有单测钉住 | `i18n-resolve.test.ts:60-63` 新增 `expect(SUPPORTED_LANGUAGES).toBe(UI_LANGUAGES)`（**`toBe` 是引用相等**，抄一份新数组即红）+ `expect(FALLBACK_LANGUAGE).toBe(FALLBACK_UI_LANGUAGE)` | ✅ 强断言 |
| 全仓确认无残留清单 | 全仓（排除 node_modules）搜 `["zh-CN","en-US"]` 形态：生产代码仅命中 `language.ts:16`（`UI_LANGUAGES`）与 `:52`（`AI_OUTPUT_LANGUAGES`，**另一套机制**，设计文档 §9.2，刻意独立且注释写明）；其余命中全在测试断言里。另搜 `"en-US"` 字面量于 `apps/desktop/src` 全目录：**仅剩 `resources.ts` 的两处登记点**（`resources` 与 `ENDONYMS`）—— 即"该登记的地方"，无一处散落兜底 | ✅ |
| 界面层的散落硬编码已收敛 | `LanguageSection.tsx` 原 `isSupportedLanguage(i18n.language) ? … : "en-US"` 改为 `: FALLBACK_LANGUAGE`；语言选项也不再由 `SUPPORTED_LANGUAGES.map` + `t()` 现拼，而是消费 `LANGUAGE_OPTIONS` | ✅ |
| `LANGUAGE_OPTIONS` 从注册表派生且被真实消费 | `resources.ts:33` `= SUPPORTED_LANGUAGES.map((code) => ({ code, label: ENDONYMS[code] }))`（原为手写两行字面量、且**生产代码零消费方**）；消费方为 `LanguageSection.tsx:55`。单测另钉「顺序随注册表」 | ✅ 已从无人消费变为有消费方 |
| **漏登记会编译失败（`satisfies` 守卫）** | 不满足于读代码，**做了一次离体实证**：在系统临时目录写一份自足 `.ts`（把注册表扩成三种语言、`resources` 与 `ENDONYMS` 各只登记两种），用仓内 `typescript@7.0.2` 跑 `--strict --noEmit`，得 **exit 1 / 3 条错误**：`TS2741 Property '"ja-JP"' is missing …Record<…,{translation:unknown}>`（resources 漏登记）· `TS2741 …Record<…,string>`（ENDONYMS 漏登记）· `TS7053`（派生 `LANGUAGE_OPTIONS` 处索引失败）。**仓库文件零改动** | ✅ 「缺一即编译失败」经实证成立 |
| 「三处」清单准确 | 新增第三语言的三处为：`locales/<tag>.json` + `i18n/resources.ts` 登记（resources 与 ENDONYMS 两个字面量，同一文件同一屏）+ 领域层 `UI_LANGUAGES`。其中 `locales/<tag>.json` 缺失则 `resources.ts` 的静态 import 直接编译失败，另两处由上条守卫兜住 —— **三处都有编译期反馈，无一处靠人记得** | ✅ |

### 1.5 系统语言检测未动（主管理员口径）

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 级联结构与宽松匹配逐字未动 | `resolve.ts` 的 diff 只有：模块注释的 `en-US`→`zh-CN`、import 领域层、`SUPPORTED_LANGUAGES`/`SupportedLanguage` 改别名、`FALLBACK_LANGUAGE` 取领域层常量、`resolveUiLanguage` 的文档注释第 3 条。**`matchSupported` 函数体（trim / 下划线归一 / 小写 / 完整标签优先 / 主语言子标签兜底）一行未改** | ✅ |
| en-GB → en-US 宽松匹配用例仍在 | `i18n-resolve.test.ts:29`（既有用例内）与 `:49`（新增用例内**再钉一遍**，注释写明「系统语言检测保留：能匹配上就仍按系统语言走，回退只是最后一环」）—— 两处 | ✅ |
| 系统语言来源未动 | `i18n/index.ts` 的 `fetchSystemLocale()`（经 IPC `app:get-locale` 取 `app.getLocale()`）与失败降级逻辑逐字未改 | ✅ |

> **口径边界，如实登记（不构成不通过理由）**：按上述实现，一台**英文系统**上的首次启动用户，
> 界面**仍以英文打开**（命中级联第二环"系统语言宽松匹配"），而非中文。用户需求原文「以中文为默认语言」
> 若按最宽读法可涵盖这种情形，本单没有覆盖它 —— 但这正是**主管理员口径明确裁定的范围**
> （"保留系统语言检测，只把最终回退 en-US → zh-CN"），交付方也在进度文档与代码注释里逐处写明了
> "本常量只是级联的最后一环"，**没有把范围说大**。故按裁定验收通过，此处仅作边界记录。

---

## 2. 代码抽查

一律**读代码核实**，不采信交付方声明自证。

### 2.1 两个「回退」确实解耦

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 两个常量物理独立 | **解析回退** = 领域层 `FALLBACK_UI_LANGUAGE`（渲染层 `FALLBACK_LANGUAGE` 为其别名）；**缺 key 回退** = `i18n/index.ts:21` 的 `const MISSING_KEY_FALLBACK_LANGUAGE: SupportedLanguage = "zh-CN";`。后者**独立声明、不引用前者** | ✅ 解耦 |
| i18next `fallbackLng` 用的是缺 key 回退常量 | `i18n/index.ts:50` `fallbackLng: MISSING_KEY_FALLBACK_LANGUAGE`（改动前是 `FALLBACK_LANGUAGE`，且该文件已**删掉对 `FALLBACK_LANGUAGE` 的 import**——不存在"两个名字指同一个常量"的隐性耦合） | ✅ |
| 注释把两者的区别说清楚了 | 三处各写一遍且互相指路：① `index.ts:10-20` 说明"某语言包缺某个 key 时用哪本顶上"、为何取 zh-CN（中文是基准语言，语言包按中文先写）、"对现有 zh/en 无任何实际影响"（parity 已保证 key 集合一致，永远走不到）、意义在将来的第三种语言；② `resolve.ts:16-19` 标注"**解析回退**"并指向领域层；③ `language.ts:24-33` 标注"**解析回退**"并指明"与 i18next 的 fallbackLng 是两个不同问题，后者在 renderer 单独声明"。另 `locales-parity.test.ts` 的模块注释也同步改为「缺失 key 静默回退基准语言（i18n/index.ts 的 fallbackLng = zh-CN）」 | ✅ 口径清楚、无孤儿注释 |
| 「对现有两语言零实际影响」属实 | `locales-parity.test.ts` 断言两包 key 集合完全一致且叶子非空（本次全绿），故 i18next 永不触发缺 key 回退 —— 此项改动对当前行为确为零影响，只是把将来的默认取值定下来 | ✅ |

### 2.2 引用方向正确（渲染层 → 领域层，不是复制）

- `resolve.ts` `import { FALLBACK_UI_LANGUAGE, UI_LANGUAGES, type UiLanguage } from "@ff-pane/shared";`
- `resources.ts` `import { SUPPORTED_LANGUAGES, type SupportedLanguage } from "./resolve";`（经渲染层门面，不越级）
- 反向：`packages/shared/src/domain/language.ts` 搜 `from "../` / `@ff-pane/core|storage|rag` / `renderer`
  —— **命中全部是注释文字**（"renderer 的 i18n/resolve.ts 直接复用它"），**零 import**。领域层未反向依赖渲染层。✅

### 2.3 endonym（`a1549ad`）

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 标签 = 各语言自己那本包的 `displayName` | `ENDONYMS = { "zh-CN": zhCN.settings.language.displayName, "en-US": enUS.settings.language.displayName }`（**各取自己那本**）；`LANGUAGE_OPTIONS` 由它派生 | ✅ |
| 单测确实钉住了"自称"的定义 | `i18n-resources.test.ts` 那条关键用例**自己从磁盘读 `locales/<code>.json`**（`readPack(code)`）再断言 `label === own.settings.language.displayName` —— 即"每个标签等于该语言自己那本包里的 displayName"。这比"断言等于某个字面量"更有证明力：若哪天改成取当前 UI 语言那本的说法，这条会红。另有三条辅助用例（覆盖全部语言且顺序随注册表 · 恒为 简体中文 / English · 标签非空且互不重复） | ✅ 有效 |
| 「界面文案逐字未变」属实 | 实读两本语言包：`zh.settings.language.displayName = "简体中文"`、`en.… = "English"`；而 `zh.settings.languageName = {"zh-CN":"简体中文","en-US":"English"}`、`en.settings.languageName` **完全相同** —— 两本的 languageName 本来就都写的是自称，故由 `t()` 改取 `ENDONYMS` 后**渲染结果逐字节相同**。交付方"上一轮交付报告曾推测『中文界面读英语』，此处以语言包为准更正"这次自我更正**是对的**，最终陈述与代码/数据一致 | ✅ 更正准确 |
| 界面语言下拉不再经 `t()` | `LanguageSection.tsx:55-59` 渲染 `LANGUAGE_OPTIONS` 的 `{label}`，无 `t()` 调用；旁边有注释说明"语言选项的名字不该随当前界面语言变" | ✅ |
| AI 输出语言侧两个消费方仍工作 | 全仓搜 `settings.languageName`：生产代码恰**两处**——`LanguageSection.tsx:83`（输出语言下拉）与 `ProfileEditorDialog.tsx:209`（Profile 输出语言下拉），**均在 AI 输出语言侧**，两处仍走 `t()`、键仍在两本语言包里（值如上）。`check-i18n` PASS 意味着被引用的键确实存在 | ✅ 未被误删、仍工作 |
| 保留该键的理由成立 | AI 输出语言（设计文档 §9.2）**不绑语言包**，故没有"自称"可取，其名字只能由语言包提供 —— 这处刻意分歧已写进 `LanguageSection.tsx:79-80` 注释。另核实：`check-i18n` 与 `locales-parity` 的检查都是单向的（"被引用的 key 必须存在 / 各包 key 集合一致"），**确无"键必须被消费"的检查**，故保留不会报错 —— 保留纯因它还有真实消费方 | ✅ 论证成立 |

### 2.4 B1：`resolveProbeKey` 挪进 `try`（上一单验收发现 ②）

`data.ts` 两个 handler 现在的结构（`:315-333` 与 `:336-352`）：

```
const outlet = resolveProbeOutlet(request.proxy);   // 可能 new ProxyAgent
if (!outlet.ok) { return outlet.failure; }
try {
  const apiKey = await resolveProbeKey(request);    // ← 已挪进来
  return await testConnection({ … });
} finally {
  await outlet.dispose?.();                          // = agent.close()
}
```

**逐条走一遍所有抛出路径**，确认"构造出来的 ProxyAgent 一定被 close"现在是**无条件性质**：

| 抛出点 | 此刻 agent 状态 | 是否走 `finally` | 结论 |
|---|---|---|---|
| `resolveProbeOutlet` 内 `new URL` 失败 / 非 http(s) 协议 | **尚未构造**（`invalidProxy` 在 `new ProxyAgent` 之前 return，读 `provider-proxy.ts:65-91` 确认） | 无需 | ✅ 无泄漏对象 |
| `resolveProbeOutlet` 内 `new ProxyAgent` 本身抛 | 构造失败，**无对象** | 无需（该 try/catch 转 `invalid-config`） | ✅ |
| `resolveProbeKey` → `revealSecret` 抛（`SecretNotFoundError` / 后端不可用） | 已构造 | **是**（这正是本次修的窗口） | ✅ 已闭合 |
| `testConnection` / `fetchModels` 抛 | 已构造 | 是（改动前即成立） | ✅ |
| 未配代理（直连）路径 | `dispose` 缺席，`outlet.dispose?.()` 可选调用为 no-op | 是 | ✅ 不误伤 |

两处均补了注释说明原因（"抛在 try 之外则已构造的 ProxyAgent 不会 close"）。
**上一单验收发现 ② 已闭环。** ✅

### 2.5 B2：`formFromProvider` 下沉 + 5 项往返单测（上一单验收发现 ③）

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 确实是下沉而非复制 | `ProviderEditorDialog.tsx` 里那 18 行函数体**整段删除**并改为 `import { formFromProvider }`；`provider-form.ts` 新增同名导出，函数体逐字相同（`provider.proxy ?? ""` 等）。**全仓无第二份定义** | ✅ |
| 落点符合仓内既有款式 | 与写出侧 `buildProviderDraft`、显隐判定 `usesProxy` 同处一个纯逻辑文件（无 React / DOM 依赖），模块注释同步补上"既有 Provider → 表单态" | ✅ |
| 5 项单测有效 | 逐条读 `provider-form.test.ts:122-160`（见下表） | ✅ 5/5 有效 |

五项逐条评估（**关心的是"漂移时会不会红"，而不是"有没有这一条"**）：

1. **全字段往返恒等** —— `expect(buildProviderDraft(formFromProvider(FULL))).toEqual(expected)`，其中
   `expected` 由 `const { id: _id, apiKeyRef: _ref, ...expected } = FULL` **解构得出**而非另写一份字面量：
   将来 `Provider` 新增字段时，`FULL` 一改，期望值自动跟上。✅ 强
2. **proxy 逐字往返 + timeoutS 数字↔字符串不失真** —— 两侧各断言一次（`state.proxy === "http://127.0.0.1:7890"`
   且 `draft.proxy` 同值；`state.timeoutS === "60"` 且 `draft.timeoutS === 60`，含类型）。
   **这正是上一单查出的那处数据损失（`providers:update` 整表单替换会抹掉手工写进 `providers.json` 的 proxy）的回归钉**：
   若读入侧哪天又漏掉 proxy，此条立刻红。✅ 钉住了
3. **缺省可选字段读为空串、写出仍省略** —— 用 `cli_login` 最小 Provider，断言四个可选字段读为 `""`，
   且 `draft` **`toEqual` 恰好四键**（`name`/`type`/`models`/`enabled`）—— 用全等而非 `not.toHaveProperty`，
   顺带排除了"凭空造出空串字段"。✅
4. **`cli_login` 的 proxy 按既有口径刻意丢弃** —— 断言读入侧**能读到**（`"http://127.0.0.1:7890"`）
   而写出侧 `"proxy" in draft === false`。把一处**刻意取舍**变成显式契约，日后若有人"顺手修好"会先红并被迫读理由。✅
5. **读入侧覆盖表单态全部键** —— `Object.keys(formFromProvider(FULL)).sort()` 与 `Object.keys(emptyProviderForm()).sort()`
   全等。**这条是形状级守卫**：今后表单新增字段而忘了在读入侧读，这里立刻变红 —— 那正是 proxy 当初丢值的形状。✅ 最有价值的一条

**上一单验收发现 ③ 已闭环，且补法与当时的建议逐字吻合。** ✅

### 2.6 文档一致性

| 核查点 | 核实方式 | 结论 |
|---|---|---|
| 设计文档 §9.1 已改并注明经用户确认 | `docs/项目设计计划-v1.0.md:607-611`：「默认值：跟随操作系统语言，无匹配语言包时回退 **zh-CN**（2026-09-01 经用户确认：产品以中文为默认语言，回退语言由 en-US 改为 zh-CN；系统语言检测保留，解析顺序仍为 已保存的选择 → 系统语言宽松匹配 → zh-CN）」，并**另起一条**「缺 key 回退：…用 zh-CN 的文案顶上（中文为基准语言）；这与上面的『解析回退』是两个问题」 | ✅ |
| 文档头部新增修订记录 | 同文件 `:10-11` 新增「**修订记录：**」块，一行记本次改动（经用户确认） | ✅ |
| §9.1 与 `domain.test.ts` 断言一致 | 文档：支持 zh-CN / en-US、回退 zh-CN、设置项可跟随系统。单测：`UI_LANGUAGES` 恰为 `["zh-CN","en-US"]` · `FALLBACK_UI_LANGUAGE === "zh-CN"` · `UI_LANGUAGE_SETTINGS` 恰为 `["system","zh-CN","en-US"]`。**逐项对上** | ✅ |
| `docs/开发进度.md` 条目与实际改动相符 | 逐句核对 §0 本单条目：两个回退常量的处置 · 收敛为一份注册表 · `LanguageSection` 兜底改 `FALLBACK_LANGUAGE` · `LANGUAGE_OPTIONS` 改派生 · resources.ts 两处登记缺一即编译失败 · E2E 固定机制走第一环故不受影响 · B1/B2 两条 · endonym 三段 · 单测 +12（1723→1730→1735）· 只审不改四条 —— **每一句都能与代码对上，未发现夸大或漏报** | ✅ |
| 「界面可见变化：无」这处自我更正 | 见 §2.3 第三行：以语言包实际数据核实，**最终陈述正确**（上一轮的推测已被自己更正，且更正方向对） | ✅ |
| 「只审不改」四条如实 | ① `DEFAULT_AI_OUTPUT_LANGUAGE` 实为 `"zh-CN"`（`shared/domain/config.ts:51`，且 `:81` 为默认配置消费），本就无需改 —— 属实；② `LANGUAGE_NAME` 实为 `Readonly<Record<AiOutputLanguage,string>>`（`core/src/prompt/language.ts:14`），加语言即编译失败 —— 属实；③ `packages/storage/config/store.ts` 无第二份语言清单 —— 属实（全仓清单搜索见 §1.4）；④ `UI_LANGUAGE_SETTINGS` / `isUiLanguageSetting` / `isUiLanguage` 在生产代码无消费方、"声明与实现分叉"未动 —— 属实（见 §4 已知边界） | ✅ 四条均如实 |

### 2.7 红线

| 红线 | 核实方式 | 结论 |
|---|---|---|
| fixtures 零改动 | `git diff 8ad784e..a1549ad -- "*fixtures*"` **输出为空**；另核两个提交的完整文件清单（15 个文件）无一位于 `packages/adapters/fixtures/` 之下 | ✅ 未破 |
| `locales/*.json` 在 `a1549ad` 里零改动 | `git diff 67471f0..a1549ad -- locales/` **输出为空**（`67471f0` 侧亦为空，见 §1.1） | ✅ 属实 |
| 改动面就是声明的那些文件 | `git diff --name-only 8ad784e..a1549ad` 共 **15 个**：产品代码 6（`main/data.ts` · `i18n/{index,resolve,resources}.ts` · `settings/LanguageSection.tsx` · `settings/providers/{ProviderEditorDialog.tsx,provider-form.ts}` 计 7 个文件中的 6 个 renderer/main 项）+ 领域层 1（`shared/domain/language.ts`）+ 测试 4 + 文档 2 —— 无依赖变更、无 lock 变更、无脚本变更 | ✅ 面很干净 |
| 密钥纪律 | B1 的改动只挪动了 `resolveProbeKey` 的调用位置，未改其实现（明文用完即弃、不进返回值）；`formFromProvider` 注释明写"apiKeyRef 刻意不进表单：密钥只以尾 4 位占位显示" | ✅ 未破 |

---

## 3. 抽查发现

**未发现声明与实现不符之处，未发现红线被绕开，未发现真实失败。**

值得记的是：本单同时闭环了上一单验收留下的两条建议（`resolveProbeKey` 挪进 `try` · `formFromProvider`
下沉后补往返单测），且**补法与当时的建议逐字吻合**，没有走"改一半"的路 —— B2 的第 5 条用例（读入侧覆盖
表单态全部键）甚至比当时建议的"往返恒等"覆盖更宽，它守的是形状而非某几个字段。

以下两条为**如实登记的口径/边界**，均不构成不通过理由（也都已在交付文档里写明，非隐瞒）：

1. **需求「以中文为默认语言」按主管理员口径只覆盖到级联最后一环。** 英文系统的首次启动仍开英文界面。
   详见 §1.5 末的边界记录。交付方在代码注释、进度文档、设计文档三处都写明了"本常量只是级联的最后一环"，
   **没有把范围说大**；且若日后要改成"一律默认中文"，改动点仍是同一个常量与同一处级联，本单的收敛让那次改动更小。
2. **`UI_LANGUAGE_SETTINGS`（含 `"system"`）在生产代码仍无消费方。** 实现用的是"localStorage 键缺省即跟随系统"，
   而非显式 `"system"` 值；设置页下拉也只给 zh-CN / en-US 两项，**用户无法显式选回"跟随系统"**（选过之后
   只能改 localStorage 或换到另一种语言）。这处"声明与实现的分叉"是既有状况、交付方如实登记为不属本单范围，
   本次核实登记文字与代码一致。**建议（不阻塞）**：若日后要让"跟随系统"成为可选项，那正是 `UI_LANGUAGE_SETTINGS`
   转为有消费方的时机。

**另记三条正面观察（非偏差）**：

- **两个「回退」的拆分是本单最有长期价值的一处判断。** 它们回答的是不同问题（"该显示哪种语言" vs "缺失文案从哪本取"），
  改动前被一个常量绑死。这次拆开时**两者恰好同值 zh-CN**，最容易的做法是"反正一样，继续复用"——那样做眼下也全绿，
  但会把一次巧合固化成依赖。交付方选择了独立声明并各写一段理由，代价是多一个常量、收益是两者今后可以各自变化。
- **「无第二份清单」用了 `toBe` 引用相等来钉。** `toEqual` 对"抄一份内容相同的数组"同样会绿，`toBe` 不会 ——
  这条守卫选对了断言强度。
- **`satisfies` 守卫经离体实证、不是靠"应该会红"。** 三个漏登记点各自给出编译错误（含派生处的 `TS7053`），
  验证在系统临时目录完成、仓库零改动。

---

## 4. 自动化回归结果

全部在仓库根执行（PowerShell，多命令以 `;` 分隔），HEAD = `a1549ad`，工作区干净。

### 4.1 全量单测 3 连跑（本次验收独立执行）

| 轮次 | 结果 | 耗时 |
|---|---|---|
| 第 1 次 | **1735 passed / 1735（85 文件）**，exit 0 | 11.68 s |
| 第 2 次 | **1735 passed / 1735（85 文件）**，exit 0 | 11.06 s |
| 第 3 次 | **1735 passed / 1735（85 文件）**，exit 0 | 10.99 s |

三次**全绿、零重试、零失败、零单文件复跑**，每次数字与交付声明的 1735 / 85 文件逐位一致。

**+12 的构成自己数了一遍**：`i18n-resolve.test.ts` +2（解析回退是 zh-CN · 注册表唯一）·
`provider-form.test.ts` +5（往返五项）· `i18n-resources.test.ts` +5（新文件，1+4）· `domain.test.ts` ±0
（既有用例内补 1 条断言、改标题，未新增 `it`）· `locales-parity.test.ts` ±0（仅改模块注释）
= **+12，1723 → 1735**，文件数 84 → **85**（+1 = 新文件）。✅ 与声明逐位一致

**flake 样本继续累积**：清债一单根治的两条 flake（`provider-probe` 坏端口 · `process` 背压）本次 **0/3 触发**；
连同此前累计，**共 14 次全量连跑无一次触发**。

### 4.2 其余回归

| 项 | 命令 | 结果 |
|---|---|---|
| lint | `pnpm lint` | **PASS** —— biome `Checked 538 files in 161ms. No fixes applied.`；`check-i18n` PASS（renderer 无硬编码 CJK） |
| typecheck（node） | `tsc -p tsconfig.node.json --noEmit --composite false` | **exit 0** |
| typecheck（web） | `tsc -p tsconfig.web.json --noEmit --composite false` | **exit 0** |
| typecheck（e2e） | `tsc -p tsconfig.e2e.json` | **exit 0** |
| 构建 | `pnpm build` | **PASS** —— 6 个工程全过（shared / rag / core / adapters / storage / desktop）；产物 main 97.95 kB · knowledge-mcp 11.34 kB · preload 3.80 kB · renderer 1467.37 kB + 35.79 kB CSS + smoke 2.60 kB |
| 冒烟 | `pnpm smoke` | **七项全过、exit 0** —— main-sqlite（SQLite 3.53.4）· ipc-ping-pong（往返 0 ms）· app-info（v0.1.0 / Electron 44.0.0）· sqlite-via-ipc · event-subscription · csp-blocks-eval · secrets-roundtrip。**`No handler registered` 零行**（实测计数为 0，清债一单成果保持） |
| E2E | `pnpm test:e2e` | **14 passed / 14**（1 worker，9.5 s） |

**E2E 的 en-US 固定机制经独立核实不受本单影响**：`tests/e2e/_launch.ts:70-76` 是
「首窗加载后写 `localStorage["ffpane.ui-language"]="en-US"` → `reload()` → 断言 `<html lang>` 为 en-US」，
命中的是**解析级联第一环（用户已保存的选择）**，与被改的最后一环无关。
三个 `scripts/live-*.mjs`（live-ten-step / live-embedding / live-knowledge-tool）同款机制，同样不受影响。
—— 与交付声明一致，且 14/14 的实测结果本身也印证了这一点。

**冒烟输出中另有一段既有噪声**（非本单引入、不影响判定）：`@napi-rs/canvas` 缺原生绑定与三条
`Cannot polyfill DOMMatrix/ImageData/Path2D` 警告。来自主进程侧 pdfjs-dist 的可选依赖，
T6.1 已决策列入 `ignoredOptionalDependencies`（本产品永不渲染位图），本单未触碰相关依赖或代码路径。

### 4.3 零外网的确认方式

本单改动面不含任何网络路径（B1 只挪动了一次函数调用的位置，未改探测实现）；新增测试全为纯逻辑与
读本地 JSON 文件（`i18n-resources.test.ts` 的 `readPack` 读 `locales/*.json`）。E2E 与冒烟一律走临时数据根。
本次验收自身也未向任何外部主机发起请求（包括未核对任何在线规范文本），§1.4 的编译守卫实证用的是仓内 `typescript`。

---

## 5. 验收结论

### **通过。**

**用户需求四条逐条成立**（第三条按主管理员口径的范围）：

- **中英双语齐全、设置页可选界面语言**（既有功能）：两本语言包 key 集合一致、下拉即时生效并持久化，
  E2E 每条用例都在走这条链路，14/14 全绿。
- **默认中文**：解析级联的最后一环由 en-US 改为 zh-CN，落点是领域层 `FALLBACK_UI_LANGUAGE`，
  渲染层 `FALLBACK_LANGUAGE` 是它的别名而非副本；「ja-JP / fr-FR 等不支持语言 → 落 zh-CN」有专用单测，
  领域层侧另补了 `toBe("zh-CN")` 的直断言（原断言 `toContain` 对 en-US 同样成立、钉不住值）。
- **可拓展「三处即全」现在成立**：改动前**两份同形清单并存**（渲染层那份被运行时消费、领域层那份零消费方），
  本单收敛为唯一注册表（领域层 `UI_LANGUAGES` 为事实源），并用 **`toBe` 引用相等**钉住"无第二份"；
  三处（`locales/<tag>.json` + `resources.ts` 两个字面量 + 领域层 `UI_LANGUAGES`）**每一处漏改都有编译期反馈**，
  其中 `satisfies` 守卫经**离体实证**（临时目录 + 仓内 tsc，三条编译错误，仓库零改动）；
  全仓确认无第二份界面语言清单残留（`apps/desktop/src` 里 `"en-US"` 字面量仅剩 `resources.ts` 的两处登记点）。
- **系统语言检测未动**：`matchSupported` 函数体一行未改，en-GB → en-US 用例在两处仍在（其中一处是新增用例
  里专门再钉的），系统语言取值链路（IPC `app:get-locale`）逐字未改。

**代码抽查其余各项均属实**：两个回退常量物理独立、i18next `fallbackLng` 用的是缺 key 回退常量、
三处注释把两者区别写清且互相指路；引用方向正确（领域层零反向 import）；endonym 侧标签确实取自
"各语言自己那本"包的 `displayName`（单测自己读盘核实、而非比对字面量），**界面文案逐字未变经语言包数据核实**
（两本的 `languageName` 本来就都是自称），交付方对上一轮推测的自我更正是对的；`settings.languageName.*`
的两个消费方（`LanguageSection` 输出语言下拉 + `ProfileEditorDialog` Profile 输出语言下拉）仍在、仍工作。
**上一单验收的两条遗留双双闭环**：B1 逐条走完五条抛出路径，"构造出来的 ProxyAgent 一定被 close"现已是
无条件性质；B2 是真下沉（原处整段删除、全仓无第二份定义）+ 5 项往返单测逐条有效，其中 proxy 逐字往返
正是上一单查出的数据损失的回归钉，而"读入侧覆盖表单态全部键"这条形状级守卫比当时的建议覆盖更宽。
设计文档 §9.1 与 `domain.test.ts` 断言逐项对上、头部新增修订记录，`docs/开发进度.md` 条目每一句都能与代码对上。

**红线未破**：fixtures 零改动（`git diff 8ad784e..a1549ad -- "*fixtures*"` 为空）；
`locales/*.json` 在两个提交里**均**零改动（声明的"a1549ad 零改动"属实，且 `67471f0` 也没动）；
改动面 15 个文件、无依赖/lock/脚本变更；密钥纪律未破。

回归数字全部达标且与交付声明一致：**全量单测独立 3 连跑均 1735 / 1735（85 文件）** ·
lint + check-i18n PASS · 三套 typecheck exit 0 · 全量构建 6 工程 PASS · 冒烟七项 exit 0 且
`No handler registered` 零行 · E2E 14 / 14。**+12 与 +1 文件的构成自己数了一遍，逐位吻合**
（i18n-resolve +2 · provider-form +5 · i18n-resources +5，domain 与 locales-parity 各 ±0）。
两条既有 flake 本次 0/3 触发，累计 14 次全量连跑零触发。E2E 的 en-US 固定机制走的是解析级联第一环，
经读码独立确认不受本单影响。

**§3 两条登记均不阻塞**：① 需求「以中文为默认语言」按主管理员口径只覆盖级联最后一环，英文系统首次启动
仍开英文界面 —— 属裁定范围，交付方三处文档均未把范围说大；② `UI_LANGUAGE_SETTINGS` 的 `"system"`
在生产代码仍无消费方、用户无法显式选回"跟随系统"，为既有状况、如实登记，建议日后要做该选项时一并收口。

**需主管理员决策事项：无。** endonym 裁定已按采纳落实并有单测钉住；语言默认的范围口径已由主管理员裁定，
本记录 §1.5 与 §3 已把其边界如实写明，供日后若要扩大范围时直接引用。
